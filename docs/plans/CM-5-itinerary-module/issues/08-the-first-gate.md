# 08: The first gate — the readers move, walked on `dev` while the old root still answers

**What to build:** the first PR, proven at the rung that ships. Every end-to-end spec, script and seeder is off the five retiring acts on the old root; the seeders publish through the new route. The full suites run with their counts read. The `dev` walk is done by hand with the pool, `t1 = owner, t2 = stranger, t3 = private owner, t4 = member`, and recorded in this ticket with which tag played which role. TW-1's still-owed device walk folds into this one, on the founder's ruling at slicing, and is ticked on TW-1's gate ticket. The PR description says what `dev` looks like the next morning — every old-flag trip unpublished and editable until its owner republishes, Discover showing the handful of real Itineraries until the seeders run — so nobody reads it as a regression. The PR is opened as the proposal and squash-merged on the founder's word; the seeders are rerun on `dev` on the founder's word.

**Blocked by:** 01, 02, 03, 04, 05, 06, 07.

**Status:** ready-for-agent

- [ ] No end-to-end spec, script or seeder names publish, unpublish, fork, audience or finish-planning on the old root; the Playwright list total is read and recorded
- [ ] Backend unit and integration suites, mobile typecheck and Jest, and Playwright on both lanes are green with their counts read from the logs and recorded here
- [ ] The `dev` walk is recorded with tags: `t1` publishes and the workspace fences; `t2` reads the page, finds it on Discover and Home, forks it; `t3`'s Itinerary is readable by `t2`; `t1` archives and `t2` loses it; `t1` unpublishes, edits, republishes and the same link resurrects at the top of Discover; an old-flag trip of `t1`'s shows unpublished and editable and one tap publishes it; the backend log lines name the itinerary module
- [ ] TW-1's owed device walk is ticked on its gate ticket with a pointer here
- [ ] The PR description carries the dark-morning note; the PR is squash-merged on the founder's word and the seeders rerun on `dev` on the founder's word
