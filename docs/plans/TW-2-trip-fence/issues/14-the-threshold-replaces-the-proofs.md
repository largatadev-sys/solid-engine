# 14: The threshold replaces the proofs

**What to build:** the mechanism change rounds 5 and 6 of the grilling decided (2026-09-19 → 22), on this branch, before PR #82 merges — because merging a mechanism already decided against is shipping debt on purpose. The **decisions do not move**: ADR-040, V58, the relocation into `trip`, the record surviving the trip, Undo. What moves is *where the two closing rules are applied*. Today the fence mints **proofs** — five nested types with private constructors — and 66 signatures carry them, every one unwrapping the proof on its first line and proceeding as before TW-2; seven are minted and discarded, `Unfrozen` has no consumer, and on those paths the fence is again a line you can delete with nothing going red. After this ticket the fence is **two `void` rules applied once at the Threshold** — one `HandlerInterceptor` on `/v1/trips/{itineraryId}` and everything beneath it — which resolves the `Membership` through the guard, applies the not-found mask for a missing membership or a closed room, reads the handler's **door**, and hands the `Membership` to the handler through `@CurrentMember` (the `@CurrentTraveler` mechanism). Services take `Membership` again; owner-only services keep `Owner`. The room's types publish as the **`trip.room`** named interface so a collaboration module depends on the room and never on the plan. **Publishing no longer closes chat** (Q30 — a product ruling, the one deliberate wire loosening). **Three acts reach a closed room** (Q29, as corrected by the build: Undo, self-leave, the edit-lock release). Grilling rounds 5–6, Q18–Q31; glossary rows *Room*, *Fence*, *Door*, *Threshold*.

**Blocked by:** 03, 04, 05, 06, 07, 08, 09, 10, 11, 12 — every code ticket; this rewrites their mechanism.

**Status:** done

## Commit plan — docs first, then one commit per module, deletions last

1. `docs(trip): TW-2 ticket 14 — rounds 5–6 on the record, the glossary's four rows, the amendments` *(this ticket, the grilling record, the glossary, the spec and design marks, ADR-011/018/039/040, epic map line 187)*
2. `feat(trip): TW-2 ticket 14 — the Threshold, @CurrentMember and @Door, unit-tested, wired to nothing yet` *(additive; every existing route still mints proofs; the interceptor is registered but no handler under it has a door, so it must be registered with default-deny OFF until step 4 — or registered on a branch-local path; the ticket's Comments record which)*
3. `refactor(trip): TW-2 ticket 14 — trip.room: the room's types move out of trip.api, allowlists follow` *(a pure move; zero assertions change; every consumer's allowlist names `trip.room..`)*
4. `refactor(trip): TW-2 ticket 14 — trip takes the threshold` *(plan, cover, dump, editing, ownership, trip; `unarchive` gains `@ReachesClosedRoom`; default-deny ON for the scope)*
5. `refactor(chat): TW-2 ticket 14 — chat takes the threshold, and publishing no longer closes it` *(the named fifth shape: 2 ITs + 2 specs flip)*
6. `refactor(poll): …` · 7. `refactor(postcard): …` · 8. `refactor(invitation): …` · 9. `refactor(join): …` · 10. `refactor(itinerary): …`
11. `refactor(trip): TW-2 ticket 14 — the proofs are deleted, and four guards replace two` *(the five proof classes, `Standing`, `TripFence.owner`, `IllegalWorkspaceTransitionException.alreadyArchived`, `ChatClosedException`; the two retired guards; the four new ones, each sabotage-checked)*
12. `docs(trip): TW-2 ticket 14 — the gate re-read: assertion-line diff, counts, the C4 and membership artifacts updated`

## Acceptance criteria

**The threshold (commit 2)**
- [x] `WorkspaceThreshold` (a `HandlerInterceptor` in `trip/room/`) matches exactly `/v1/trips/{itineraryId}` and `/v1/trips/{itineraryId}/**` and nothing else — pinned by a unit test that feeds it the routes of `/v1/trips` (list, create), `/v1/itineraries/{objectId}`, `/v1/itineraries/{id}/diary/entries`, `/v1/join/{token}` and asserts none are matched
- [x] For every matched request, GET included: `guard.requireMember(traveler, itineraryId)` (mask on none) → `fence.requireOpenRoom(itineraryId)` (mask on a closed room, skipped only under `@ReachesClosedRoom`) → the handler's `@Door` read → `fence.requireUnfrozen(itineraryId, door.refusal())` for `EDITABLE` / `MEMBERSHIP_MUTABLE` → the `Membership` stored as a request attribute. Order pinned by test: a non-member on a deleted trip hears the mask, an owner on a deleted trip hears the mask, a member on a published trip through an `EDITABLE` door hears `ITINERARY_PUBLISHED`
- [x] `@CurrentMember Membership` resolves from the attribute (`HandlerMethodArgumentResolver`, registered beside `CurrentTravelerArgumentResolver`); a handler under the scope that declares it and is reached without the threshold having run fails loudly, never silently null
- [x] `@Door` is one annotation with one enum: `OPEN` (room only) · `EDITABLE` (→ `ItineraryPublishedException`) · `MEMBERSHIP_MUTABLE` (→ `MembershipFrozenException`). **No `refusal` attribute** — Q30 removed its only user
- [x] A non-GET handler under the scope with no `@Door` → `IllegalStateException` naming the handler method, surfacing as 500 — pinned by test against a branch-local undeclared handler that is deleted before the PR (the ticket-11 window pattern)
- [x] `@ReachesClosedRoom` skips only the closed-room refusal; membership and role are still demanded. ~~**Exactly one act carries it: `unarchive`**~~ **Three acts carry it, and the build found two of them** — `unarchive` (Undo), `endMembership` (self-leave: spec user story 8, *"a closed room never traps my membership"*, asserted by three ITs the threshold commit turned red) and the edit-lock `release` (asserted by `ArchiveWriteFenceIT.releasingTheEditLockIsNotFencedThoughAcquiringIs`). The rule they share: **an act that withdraws the caller's own footprint reaches a closed room.** Removing *another* member is not withdrawal, so `depart`'s removal branch keeps the room and freeze checks explicitly in the service
- [x] `@PublicFace` — the scope's one exception, found by six red ITs: `GET /v1/trips/{tripId}/itinerary` is the **published page addressed by trip id**, readable by any traveler once published. It is not the room's; the threshold steps aside for a handler that declares it. Exactly one handler carries it (`ItineraryController.readByTrip`); the count guard at commit 11 pins it

**The room's interface (commit 3)**
- [x] `trip/room/` holds `Membership`, `Role`, `Owner`, `AuthorizationGuard`, `MembershipResolver`, `TripFence`, `ArchiveState`, `PublicationState`, `MembershipApi`, `MembershipView`, `MembershipArrived`, `MembershipEnded`, `TripArchived`, `WorkspaceThreshold`, `CurrentMember`, `Door`, `ReachesClosedRoom` — `package-info.java` declares `@NamedInterface("room")`
- [x] Every consumer module's boundary guard names `trip.room..` where it named `trip.api..` for these types; `chat` and `poll` name **only** `trip.room..` and `trip.exception..` — measured before and after and written to the Comments
- [x] The move changes **zero assertions** (the assertion-line diff over this commit alone is empty, as ticket 02's was)

**The consumers (commits 4–10), each commit's Comments recording its counts**
- [x] Every handler under the scope: `guard.requireMember` and every `fence.*` mint removed; `@CurrentMember Membership member` in the signature; write handlers carry a `@Door`; owner-only handlers keep `Owner.of(member, refusal)` and their services keep `Owner`
- [x] Every service that took a `TripFence.*<…>` proof takes `Membership` (or `Owner`) — ~45 signatures; `EditLeaseService.requireHeldBy` and its siblings take `Membership`
- [x] The outliers call the fence explicitly and are exactly Q24's list plus the one the build added (`depart`'s conditional removal branch — four files, pinned by `TheFenceOutliersAreNamedTest`): `JoinService.request` (`requireUnfrozen(tripId, LinkClosedException::new)` — and the hand-written archive check at `JoinService:208` becomes `requireOpenRoom`'s sibling call with the same refusal, so the hand copy dies), `InvitationService.accept` (`requireUnfrozen`), `InvitationService.revoke` (`requireOpenRoom` + `requireUnfrozen(…, MembershipFrozenException::new)` after resolving the trip), `DiaryController` / `DiaryService` (legacy, `requireOpenRoom`). `isClosed` in join stays a read (ticket 09's argument holds)
- [x] `archive` on a deleted trip and `destroy` on a deleted trip answer the mask (Q29) — one new IT each, asserting `ITINERARY_NOT_FOUND`
- [x] **Chat opens (Q30):** `ChatController.send` takes `@Door(OPEN)`; `ChatDeliveryIT`, `ChatContractIT`, `e2e/api/chat.spec.ts`, `e2e/web/chat.spec.ts` flip from asserting `CHAT_CLOSED` on a published trip to asserting delivery — **these four are the named fifth shape** in the gate's assertion-line diff and are listed by line on this ticket
- [x] The controller ITs of every module **other than chat** pass unedited beyond import lines — the neutrality proof, as at ticket 03

**The deletions and the guards (commit 11)**
- [x] Deleted: `TripFence.InAudience/Writable/Editable/MembershipMutable/Unfrozen`, `Standing`, `TripFence.owner(...)`, the generic parameter on every door, `IllegalWorkspaceTransitionException.alreadyArchived()` (nothing asserts it — measured), `ChatExceptions.ChatClosedException`, `Proofs` (test support), `OnlyTheFenceMintsItsProofsTest`, `StandingIsMintedAndDemandedTest`
- [x] `TripFence` is two ports and three `void` methods — `requireOpenRoom(UUID)`, `requireOpenRoom(UUID, Supplier)` (join's link needs its own refusal for a closed room), `requireUnfrozen(UUID, Supplier)` — and `TripFenceTest` covers both with lambdas: open/closed × published/unpublished, each refusal by type
- [x] Kept: `OnlyTheFenceRefusesTest`, `ArchiveDominatesPublishCoverageTest`, `WorkspaceStateOpenOrArchivedIT`; ~~`AudienceFenceCoverageTest`~~ retired at commit 4 (its scan keyed on the door calls the threshold removed — see the Comments)
- [x] Added, each **sabotage-checked with the failing line in the Comments**, and the first sabotage distrusted (ticket 11's lesson): `EveryWriteUnderTheThresholdDeclaresADoorTest` (reflection over `@RestController` handlers whose route resolves under the scope; every non-GET carries `@Door`) · `OnlyTheThresholdResolvesMembershipUnderItsScopeTest` (no controller under the scope names `guard.` or `fence.`) · `OnlyWithdrawalReachesAClosedRoomTest` (`@ReachesClosedRoom` is exactly {unarchive, endMembership, release}, and `@PublicFace` exactly {readByTrip}) · `TheFenceOutliersAreNamedTest` (every `fence.require*` call outside `trip/room/` is in Q24's list, by file — a ratchet that prints the list and fails on growth)

**The gate re-read (commit 12)**
- [x] The assertion-line diff over `origin/dev...HEAD` reports its changed assertions in **five** categories: ticket 03's four, plus *chat opens at publish* (Q30) — every line of the fifth named here; nothing unexplained
- [x] CI green with the `Tests run:` counts read from the log; Playwright `--list` `Total:` read; the PR's Playwright run green
- [x] Tickets 03, 04, 06, 07, 08, 09, 10, 11 each carry one dated line: *"Mechanism replaced at ticket 14 (rounds 5–6); the decisions on this ticket stand."*
- [x] The two artifacts (the C4 model, the membership model) redrawn from the tree as built; the PR body gains a section naming the mechanism change and the one wire loosening
- [x] BUILD_STATUS row unchanged (✅ + spec link); no off-epic entry (this is TW-2's own work)
- [x] `git diff --name-only` empty before each commit; staged paths compared both ways against this ticket's edits

## Comments

**Commits 2 and 3 swapped (2026-09-22).** The `trip.room` move went first (`28073b8d`) so the threshold was written against its final package. The move: 14 types, 6 tests, 83 import rewrites, 5 JPQL string literals in `MembershipRepository` that no import sweep can see; **0 pre-existing assertions changed across 23 test files.** The `mytrips` guard caught a transitive reach on the first run — `MyTripResponse.of` calls `Role.wireName()` through `TripListEntry` — which is the guard doing exactly its job; `mytrips` now allows `trip.room` beside `trip.api`. `chat` and `discovery` allow the room and never `trip.api`; `poll` narrowed from the whole module to room + exception.

**The threshold (`801793be`).** Its first test run found a design gap: Undo is a write that reaches a closed room, and reaching a closed room does not excuse it from declaring a door — so `unarchive` carries `@Door(OPEN)` **and** `@ReachesClosedRoom`, two orthogonal declarations, pinned by test. **Its CI run went red on 17 ITs across 11 classes**, and every one of them is a fact the grilling did not have — recorded here rather than smoothed over:

| Group | ITs | What the red said | What changed |
|---|---|---|---|
| The mask has one code | 4 (`ItineraryContractIT`, `TripDayPostcardContractIT`, `TripDestructionContractIT` ×2) | the new-world routes under `/v1/trips/**` answered a stranger `TRIP_NOT_FOUND` where the trip's own routes answer `ITINERARY_NOT_FOUND`; the threshold's mask runs first and unifies them | the four assertions flip to `ITINERARY_NOT_FOUND`. **Client impact: none, measured** — the app reads `TRIP_NOT_FOUND` in four places (`ScreenMessage`, `membershipErrors`, `pollMessages`, `removalFailure`) and every one treats the two codes as one |
| Deleting a deleted trip (Q29) | 3 (`TripArchiveContractIT`, `ArchivedTripListIT`, `TripDestructionContractIT.destructionWorksFromTheArchivedStateToo`) | `archive` on an archived trip answered 409 `ILLEGAL_STATE_TRANSITION`, and `destroy` on an archived trip **succeeded** — the round-6 reading said nothing asserted either; **two ITs asserted the first and one the second** | all three flip to the mask; `destructionWorksFromTheArchivedStateToo` becomes `destroyingADeletedTripIsNotAnAct_theMaskAnswersItsOwner` and asserts the row survives — *the bin story owns scheduled destruction of deleted trips* |
| Withdrawing reaches a closed room | 4 (`AudienceLadderIT`, `ArchiveWriteFenceIT` ×2, `PublishFreezesMembershipIT`) | self-leave and edit-lock release on an archived trip answered 204 — spec user story 8 and decision 9's bare-standing list, which the round-6 reading had narrowed to Undo alone | `endMembership` and `release` gain `@ReachesClosedRoom`; the removal branch of `depart` keeps `requireOpenRoom` + `requireUnfrozen` explicitly, so an owner still cannot remove a member from a deleted or published trip |
| The trip's public face | 6 (`ForkProvenanceIT` ×4, `ActivityPhotoContractIT`, `AudienceLadderIT`) | `GET /v1/trips/{tripId}/itinerary` — the published page by trip id — is readable by any traveler, and the threshold demanded a membership | `@PublicFace` on the one handler; the threshold steps aside for it |

**Commit 4 — trip takes the threshold.** Seven controllers rewritten (`TripController`, `DayController`, `PlanController`, `ActivityController`, `PhotoDumpController`, `EditLeaseController`, `TripMembershipController`) plus `TripDestructionController`: `guard.requireMember` and every `fence.*` mint gone from all of them; every write declares a door; owner-only acts mint `Owner.of(member, refusal)` in the handler. **30 service methods** across nine services take `Membership` (or `Owner`) again, by a script that walked each method, read the proof's kind, dropped the unwrap line and repointed later uses — zero hand edits in the services beyond `depart`'s branch and `PhotoDumpService` losing a fence dependency it no longer had a use for. Eight ITs that minted through `Proofs` now pass the membership or `asOwner(member)` directly; their assertions are untouched. **Two guards retired here rather than at commit 11**, because their premise died with this commit and a failing guard is worse than a deleted one: `StandingIsMintedAndDemandedTest` (services take bare standings *by design* now) and `AudienceFenceCoverageTest` (it scanned GET bodies for door calls the threshold removed; its third test — *handlers that serve non-members are named* — is what `@PublicFace`'s count guard becomes). Two new ITs: `deletingADeletedTripIsNotAnAct_theMaskAnswersItsOwner` (`TripReadContractIT`) and the destroy one above. Door tally in trip, measured: `EDITABLE` 18 · `OPEN` 8 · `MEMBERSHIP_MUTABLE` 4 (30 doors for 31 writes — the one undeclared is `POST /v1/trips`, outside the scope by design) · reads 3 · `@ReachesClosedRoom` 3 · `@PublicFace` 1 (in `itinerary`, touched here because the threshold's consequence is this commit's).

**Commit 5 — chat takes the threshold, and publishing no longer closes it (Q30).** `ChatController.send` takes `@Door(OPEN)`; `thread` is a read. `ChatService.send`/`thread` take `Membership`. **`ChatClosedException` is deleted** and `chat` imports nothing from the fence — its allowlist already named only `trip.room` and `trip.exception`. **The named fifth shape lands here, four flips exactly as round 6 predicted:** `ChatContractIT.publishingClosesChatForOwnerAndMemberAlike` → `publishingDoesNotCloseChat_theOwnerAndTheMemberStillSend` (two 409s become two 201s); `ChatDeliveryIT.aRefusedSendBroadcastsNothing…` keeps its point with a refusal that still exists — a stranger's 404 — and gains a twin, `publishingDoesNotCloseChat_aSendOnAPublishedTripIsDelivered`, which asserts the bridge carries the frame; `e2e/api/chat.spec.ts` and `e2e/web/chat.spec.ts` flip from asserting `CHAT_CLOSED` to asserting 201. `OnlyTheFenceRefusesTest`'s prose named chat's refusal as the overload's example; it names join's now. **One thing Q30 did not rule on, flagged rather than decided:** the client's published-trip screen redirects to the published page, which has no chat surface — so the *API* is open on a published trip while the *app* offers no door to it. The web spec's old title said as much ("a published trip has no chat door") and asserted nothing about it; the UI is unchanged in this story and is the founder's call.

**Commit 6 — poll takes the threshold.** Four writes, all `OPEN` (they were `writable` — room only — and a poll survives publication); `board` is a read. Five `PollService` methods take `Membership`; `delete` and `close` keep `requireAuthorOrOwner` exactly where it was — *author-or-owner* is a fact about the poll, not a standing, and the act decides it. Two ITs (`PollLazyCloseIT`, `PollVoteRaceIT`) drop their proof helpers; assertions untouched. `poll`'s allowlist already named only the room and the refusals.

**Commit 7 — postcard takes the threshold.** The two trip-rooted creation routes (`/v1/trips/{tripId}/days/{dayId}/postcards`, `…/activities/{activityId}/postcards`) take `@Door(OPEN)` and `@CurrentMember`; their private `requireMember` helpers — the ones that answered `TRIP_NOT_FOUND` — are gone, and with them the module's last use of `TripNotFoundException` at the wire. `PostcardService`'s two methods and `DiaryService`'s eight take `Membership`. **The legacy `DiaryController` (`/v1/itineraries/{itineraryId}/diary/entries`) is Q24's first named outlier**: its prefix carries object ids elsewhere and it sunsets at CM-5, so it stays outside the threshold and asks for the room itself — one private `inTheOpenRoom(traveler, itineraryId)` doing `guard.requireMember` then `fence.requireOpenRoom`, called once per handler, which preserves ticket 05's posture for the old-world route byte for byte (the record's *new-world* routes under `/v1/diaries/**` were already fence-free by Q17). No postcard test touched the fence.

**Commit 8 — invitation takes the threshold.** `invite` and `inviteByHandle` declare `MEMBERSHIP_MUTABLE`; `pendingInvitations` is a read — its discarded `inAudience` (one of the seven) is simply gone, the threshold having done the work. `InvitationService.invite`/`inviteByHandle` take `Membership`. **Two of Q24's outliers live here, keyed by invitation id rather than trip id:** `revoke` resolves the trip after loading the invitation and then asks `guard.requireMember` → `fence.requireOpenRoom` → `fence.requireUnfrozen(…, MembershipFrozenException::new)` — the same three facts its discarded `membershipMutable` mint proved, now as three named checks; `accept` asks `requireUnfrozen` only, because a live invitation to an archived trip cannot exist (`ArchiveVoidsInvitations`, BEFORE_COMMIT). `InvitationExpiryIT` drops its proof helper.

**Commit 9 — join takes the threshold, and the hand copy dies.** Four handlers declare `MEMBERSHIP_MUTABLE` — **including the two reads** (`link`, `queue`), because a published trip's roster is frozen and `JoinLifecycleIT:461` asserts the link answers `MEMBERSHIP_FROZEN`; a door on a GET is honoured by the threshold exactly as on a write, and that is the honest declaration of what those reads were already doing through `membershipMutable`. `queue`, `approve`, `decline` mint `Owner.of` in the handler; `JoinService`'s four methods take `Membership` or `Owner`. **`request` (by token) is Q24's outlier**, and the design's table of hand copies is finally empty: `JoinService:208`'s hand-written `if (workspaces.isArchived(…)) throw new LinkClosedException()` becomes `fence.requireOpenRoom(itineraryId, LinkClosedException::new)` — the fence gained that refusal-carrying overload for it — beside `requireUnfrozen` with the same refusal. `isClosed` stays a read (ticket 09's argument holds).

**Commit 10 — itinerary takes the threshold.** `publish`/`unpublish` declare `OPEN` (they were `writable` — the act's own service decides what publish means on a published trip) and mint `Owner.of`; `preview` is an owner-only read minting `Owner.of` for `snapshotOfLivePlan`. `readByTrip` keeps its `@PublicFace` from commit 4. The object-addressed routes (`read`, `fork`, `destroy` of an object) are outside the scope and untouched. `ItineraryObjectService`'s three methods take `Owner`. The controller's private `requireMember` — the last `TripNotFoundException` at the wire — is gone.

**Commit 11 — the proofs are deleted, and four guards replace two.** `TripFence` is two ports and three `void` methods (`requireOpenRoom` ×2, `requireUnfrozen`); the five proof classes, every door, `TripFence.owner`, `Standing` (and its `membership()` overrides on `Membership` and `Owner`), `UndeclaredWrites` (the window — the threshold now always refuses an undeclared write), `IllegalWorkspaceTransitionException.alreadyArchived()` and the `Proofs` test support are gone. `TripFenceTest` is rewritten for the void fence (8 tests: the two rules, their independence, the refusal not consulted when a rule opens, both facts required). `OnlyTheFenceRefusesTest`'s pin that the fence names the two default refusals moves to the threshold, which is where they are named now. **Four guards added, each sabotaged once and caught with the right diagnosis:** `EveryWriteUnderTheThresholdDeclaresADoorTest` (a `@Door` removed from `PollController.ask` → *Expecting empty but was: [PollController#ask]*; the scan pins ≥ 40 writes and names eight by module so it cannot pass on an empty scan) · `OnlyTheThresholdResolvesMembershipUnderItsScopeTest` (`guard.requireMember(` planted in a controller → named by file) · `OnlyWithdrawalReachesAClosedRoomTest` (the `@ReachesClosedRoom` removed from `release` → the exact list of three fails; the same class pins `@PublicFace` to exactly one) · `TheFenceOutliersAreNamedTest` (`fence.requireOpenRoom` planted in a non-outlier → the exact list of four fails). They share `support/RoutesUnderTheThreshold`, which composes each handler's route from the class prefix and the merged mapping and filters to *a trip named by its id*. **Counts read, not trusted:** 32 tests across the seven `trip/room` test classes, none vacuous.

**Commit 11's CI: 1352 ITs, 2 red, both mine, both exact.** `TripContractIT.aMalformedItineraryIdIsABadRequestNotAServerError` — a trip route whose id is not a UUID answered 500 where it answered 400 `MALFORMED_REQUEST`: the threshold's parser treated a non-UUID segment as *not a trip route* and passed it through, so the handler's `@CurrentMember` found nothing and threw. A trip route with a bad id is still a trip route; the threshold now answers `MalformedTripIdException`, a `ValidationException` carrying the same code and message the `@PathVariable UUID` conversion used to, so the wire is unchanged (no static segment lives under `/v1/trips/`, measured, so the stricter parse rejects nothing real). `ArchiveWriteFenceIT.everyFencedWriteFamilyAnswersAMemberWithTheMask` — a member removing the *owner* from an archived trip heard 403 before the mask: `depart`'s removal branch had `Owner.of` before `requireOpenRoom`, where the door it replaced checked the room first. Reordered; the mask is outermost there too, as Q11/Q14 require everywhere.

**Commit 12 — the gate, read from the log (2026-09-22).** Push run on `aad40ab2`: backend **1352 integration + 536 unit, zero failures**; the clean-checkout `compose up` smoke green. PR run: **Playwright 851 passed, 1 flaky, 5 skipped** of the 857 `--list` reports; mobile **6845 jest, `tsc` clean**; no client source changed in this ticket. The one flaky spec is `e2e/web/discovery.spec.ts:236` — the same untouched spec that was flaky-then-passed on the pre-ticket PR run; this ticket names no path in `discovery`, so it is recorded as the recurring flake it already was, not as this ticket's. The five skips are the suite's conditional skips (a crawler probe that skips without a preview container among them), unchanged. Quarantine ledger: empty, no row added. **The assertion-line diff over ticket 14's span (`b8d5d730` → `aad40ab2`): 29 pre-existing assertion lines changed across 27 compared files, 7 new test files, 5 deleted — every line in one of these categories:** *the mask has one code* (4: `ItineraryContractIT`, `TripDayPostcardContractIT`, `TripDestructionContractIT` ×2 — `TRIP_NOT_FOUND` → `ITINERARY_NOT_FOUND`) · *deleting a deleted trip, Q29* (3: `TripArchiveContractIT`, `ArchivedTripListIT`, `TripDestructionContractIT` — 409/204 → the mask) · *chat opens, Q30* (`ChatContractIT` 68→63 lines, `ChatDeliveryIT` 27→29, plus the two Playwright specs) · *a proof helper became the membership itself inside an assertion lambda* (14: `EditLeaseExpiryIT` ×7, `LifecycleRespectsEditingSessionIT` ×3, `ActivityStorageIT` ×2, `TripLifecycleStorageIT`, `InvitationExpiryIT` — the asserted act and outcome untouched) · *the fence's own test rewritten for the void fence* (`TripFenceTest` 63→17 lines, moved to `trip/room`) · *guards retired* (`Proofs`, `AudienceFenceCoverageTest`, `OnlyTheFenceMintsItsProofsTest`, `StandingIsMintedAndDemandedTest`; `OnlyTheFenceRefusesTest` ×2 — its prose and its pin moving to the threshold) · *new tests* (`TripReadContractIT` +5, `TripDestructionContractIT` +5, `WorkspaceThresholdTest`, `CurrentMemberArgumentResolverTest`, the four guards). Nothing unexplained. The two artifacts (the C4 model, the membership model) are redrawn from the tree at this commit; PR #82's title and body carry the mechanism change and the one wire loosening; BUILD_STATUS's row is unchanged; `git diff --name-only` was empty before every commit and the staged set compared both ways each time.

**`/code-review` on the ticket-14 span (`b8d5d730…HEAD`), 2026-09-22 — both axes, findings taken.** *Standards:* one hard violation — `WorkspaceThreshold` and `CurrentMemberArgumentResolver` were `public` with nothing outside `trip.room` naming them (06b §11); both are package-private now, as is the request attribute's name. Judgement calls taken: the `Owner theOwner` / `Membership owner` naming the conversion script introduced in nine methods is `Owner owner` / `Membership member` throughout (renamed outside string literals, so no log line moved); the fence fake duplicated in `TripFenceTest` and `WorkspaceThresholdTest` and the `read(Path)` helper duplicated in two guards live once each in `support/Fences` and `support/Sources`; `RoutesUnderTheThreshold` no longer defaults a mapping with no HTTP method to GET — it counts as a write, so a bare `@RequestMapping` under the scope would be held to the door rule rather than exempted. Declined, with the reason: `depart`'s discarded `Owner.of(caller, …)` stays — it is the repo's *throws or exists* idiom (Q16), and a `void` twin of it for one caller would be worse than the one line; the threshold constructed in `TripRoomWebConfig` rather than published as a `@Bean` stays — `IdentityWebConfig` is the precedent and nothing injects it; the `named.isEmpty()` branch in the threshold is unreachable in production (the interceptor fires only under its patterns) and stays as the tested, cheap second lock on the scope. Six imports the conversion left behind were removed the commit before (found by a scan of every import in the 171 touched files). *Spec:* implemented faithfully, no code defect; three places where one copy of the record had not followed the build's corrections — the grilling's settled model still said *only Undo*, this ticket still named the closed-room guard by its planned name and still listed `AudienceFenceCoverageTest` as kept — all corrected in the same commit. The spec axis independently confirmed the `@PublicFace` route was already membership-free before the ticket (so the annotation preserves behaviour rather than loosening it) and that every client read of `TRIP_NOT_FOUND` treats it as `ITINERARY_NOT_FOUND`.
