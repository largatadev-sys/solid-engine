# 06 — Gate

**What to build:** the story's closing verification and the promotion proposal — nothing new ships here.

**Blocked by:** 01, 02, 04, 05.

**Status:** ready-for-agent

- [ ] The device walk: swipe feel and the peek on real touch, Reduce Motion, the toast inset clearing Plan a Trip, sheet swipe-dismiss — the classes no suite reaches.
- [ ] The closing broad sweep: the toast is shared code now, so full `npx jest`, full Playwright, and the backend suite once, at the end (the shared-code rule).
- [ ] BUILD_STATUS row updated (status + spec link, nothing else) in the last commit on the feature branch.
- [ ] The PR to `dev` opened as the promotion proposal — never merged unasked; CI read, `Tests run:` counts included in the report.
- [ ] Anything surfaced along the way that outlives the story is on the epic map's backlog, not in a comment.
