# 05 — The walk and the record

**What to build:** the story proves itself on the rungs that ship and leaves the record straight: one Playwright spec walks the entire fork loop against the real local stack, the domain docs absorb what the grilling decided, and the trackers close out on the branch.

**Blocked by:** 03 — The traveler forks · 04 — Attribution lives where the trip lives.

**Status:** ready-for-agent

- [ ] A new fork spec in the Playwright web project walks the loop with verified-pool identities: seed a published itinerary through real flows, sign in as a second traveler, CTA → sheet (copy asserted) → Fork It → success (pill, placeholder thumb) → Open Trip Workspace (subtitle renders) → back lands on Trips → the draft sits in Trips' Draft section → the source page shows the incremented Forked count
- [ ] Docs ride the branch: the glossary gains the **Fork** verb entry; the Fork Relationship entity row gains the one-hop + live-identity note; the epic-map chooser-activation line closes as discharged by the CTA entry; S4.9 note *(a)*'s Tentative-Dates field is recorded as superseded (zero-input fork, dates workspace-private since S4.25)
- [ ] BUILD_STATUS row for S4.7 updated on the feature branch — status + spec link, nothing else
- [ ] The gate runs once, before proposing promotion: full mobile suite, full backend ITs (counts from the summary, never the exit code), and the whole Playwright suite (`npm run smoke`, one exit code)
- [ ] Device walk on the emulator dev build: fork a seeded published itinerary end-to-end, state which pool tag played which role, screenshot the success screen and the workspace subtitle

## Comments
