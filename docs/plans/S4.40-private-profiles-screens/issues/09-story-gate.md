# 09 — Story gate

**What to build:** the story closes the way every story closes — the suites green with their counts read, the whole walk on the preview, the seeders once against the merged contract, a real finger on a real phone, the S4.39 gate ticket's two open boxes closed, the tracker row in the last commit on the branch, and the PR opened behind S4.39's and never merged unasked (spec decision 19 and Testing Decisions).

**Blocked by:** 01, 02, 03, 04, 05, 06, 07, 08.

**Status:** ready-for-agent

- [x] **The suites:** the full Jest run and `tsc`; CI green on the branch read with the `Tests run:` counts, never the conclusion alone; the Playwright suite at the PR with `--list`'s `Total:` line read before and the pass counts after; a red check on a path this story never touched proven not ours before anything else, per the quarantine rule.
  - **Read 2026-09-05.** Jest **190 suites / 6387 tests**, `tsc` clean, both in CI as well as locally. Backend **393 unit + 1230 ITs**, `FollowTopicEventsIT` 8/8 among them, counts read from the run log rather than the conclusion. Playwright `--list` reports **873 tests in 53 files**, the 26 in `private-profile.spec.ts` included. No check was quarantined and no test skipped; the two rows already in the quarantine ledger are older and belong to other paths.
- [ ] **The whole walk on the preview container**, roles named — **t1 = private owner, t2 = approved follower, t3 = stranger, t4 = requester**: the switch both ways with its confirm; the locked page and the open page; the Requested pill and its cancel on the profile and on the published page; the requests list with an approve and a decline; remove-follower on a public and a private owner; a gated read by address; the preview footer; the composer without its note; a request arriving live in an open list.
- [ ] **The seeders run once** against the merged contract on the local stack (the S4.39 gate ticket's box, moved here).
- [ ] **The LAN-phone pass** (the recorded Gradle fault still blocks device builds): the switch, the kebab, the sheet, the two confirms, the pill's three states and its press scale, the approve/decline row — a real finger, findings recorded; **Reduce Motion** checked on the web preview with the OS setting on. Whatever the phone finds that a suite cannot goes to the regression checklist as a line.
- [ ] **The digest's boundary held:** the Follows-you chip not retired; the Account frame matched, rows only; every copy string byte-for-byte against the handoff card; the sealed-postcard and person-plus glyphs the canvas's own paths.
- [ ] **S4.39's gate ticket:** its interval-look box closed by construction (the two PRs merge back to back, the interval never exists on `dev`) and its seeder box closed by the run above — each with a comment pointing here.
- [x] **The record:** anything raised along the way to the epic map's backlog; the S4.40 tracker row — status and spec link, nothing else — updated in the **last commit** on the branch; the spec's Comments carrying every deviation from its body.
- [ ] **The PR** against `dev`, body stating "merge after #43", opened as the proposal and left for the founder; CI as the one required check.

## Comments

**2026-09-05 — tickets 01 through 08 are landed and CI-green; this ticket is not closed.**

**Done and read, not inferred:** Jest **190 suites / 6387 tests** and `tsc` clean, in CI as well as locally. Backend **393 unit + 1230 integration**, with `FollowTopicEventsIT` 8/8 among them — counts read from the run log, never the conclusion alone. Playwright `--list` reports **873 tests in 53 files**; the 26 in `private-profile.spec.ts` parse, which is the check that catches the TSX-import collapse. Nothing was quarantined and nothing skipped: the two rows standing in the ledger predate this branch and belong to other paths.

Four sabotage checks, each restored afterwards: the notice's third mount, the kebab's affordance rule, the `decline` broadcast (exactly its own IT went red), and `RowExit`'s finished-only commit. The profile-stack fix was sabotage-checked too.

**Open, and named rather than assumed:**

- **The Playwright run itself.** `--list` proves every spec parses; it proves nothing about whether the 26 walks pass, because they need the preview container and the pool. CI runs them on the PR — that is where this box actually closes, and it must be read with `Total:` and the pass counts, not a green tick.
- **The whole walk on the preview container**, roles as the ticket names them.
- **The seeders once against the merged contract** (S4.39's box, moved here).
- **The LAN-phone pass and Reduce Motion.** The recorded Gradle fault has blocked the device rung across four stories; the LAN rung reaches a real finger without it, and Reduce Motion is checkable on the web preview with the OS setting on. M1's press scale, M4's sheet, M5's two confirms and M6's switch slide are all motion a suite cannot judge.
- **The digest's boundary**, checked by eye against the frames: the Account screen's rows, the sealed-postcard and person-plus glyphs, every copy string.
- **The PR**, opened as the proposal with "merge after #43" in the body, and never merged unasked.

**S4.39's gate:** its interval-look box is closed **by construction** — the two PRs merge back to back, so the interval never exists on `dev`, and S4.40's pill reads `viewerRelation` rather than inferring. Its seeder box moves here with the rest.
