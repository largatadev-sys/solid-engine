# 05 — Story gate

**What to build:** nothing new — the closing pass. Full local Jest (the story added files
under `src/`, so the scoped loop's blind spot applies), `npx playwright test --list` reads the
`Total:` line, CI green on the branch (read the `Tests run:` counts, not the conclusion), and
the rung no suite reaches: a real finger on the LAN rung tapping a location and landing in the
real Google Maps app/site — the one behavior `window.open` capture cannot prove. The recorded
workstation Gradle fault does not block this (JS-only story, walkable through Metro and the
web preview; budget the known fault per the gotcha if a device build is attempted anyway).
Then BUILD_STATUS's PL-1 row goes ✅ in the last commit on the feature branch, and the PR to
`dev` opens as the promotion proposal — never merged unasked.

**Blocked by:** 02, 03, 04.

**Status:** ready-for-agent

- [ ] Full Jest suite green locally; Playwright `--list` totals read and sane
- [ ] CI green on the branch with `Tests run:` counts read from the log
- [ ] LAN-rung walk: a tap on each surface family opens Maps on the phone (state which pool tag played which role)
- [ ] Anything surfaced along the way is captured on the epic map, not left in conversation
- [ ] BUILD_STATUS row → ✅ in the final feature-branch commit
- [ ] PR opened to `dev` with the story id in the title; merge awaits the founder
