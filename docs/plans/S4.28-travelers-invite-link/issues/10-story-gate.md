# 10 — The story gate

**What to build:** nothing — the closing pass: the device rung, the record put straight, and the PR proposed.

**Blocked by:** 09.

**Status:** ready-for-agent

- [ ] **Device walk** (dev build on the AVD against the local stack): the motion contract by eye — the M6 cascade playing once per visit and not on sheet return, M2 closes, the postcard entering as one unit, Reduce Motion jump-cutting (toggle the system setting) · the **real platform share sheet** opening from the link row (dismiss the LogBox banner before tapping the bottom rail — the S4.19 trap) · the shared URL opened from a **real chat app once**: browser → web landing.
- [ ] **Phone-frame layout pass**: the roster rows, the sheet, the offer card and the postcard on the device viewport (the S3.1 truncation class is invisible on the wider preview).
- [ ] Suites read from CI (`gh run list` / `watch` / `view --log-failed`): the push jobs green and the merge-gate Playwright job green, **`Tests run:` counts read, never the conclusion alone**.
- [ ] BUILD_STATUS: the S4.28 row flips (status + spec link, nothing else) **in the last commit on this branch**.
- [ ] The record checked: the four parked epic-map lines (suggestions · link reset · guests · live updates) read correctly against what shipped · the candidate-capability note (`invitation.send`, `invitation.link`) sits in ADR-009's accumulating map · the spec's **archived-Leave flag** is discharged with the founder's ruling recorded (the build applied the spec's recommendation — own-row Leave stays on archived trips — unless the review overruled it).
- [ ] The mock originals (`Travelers Spec.dc.html` + `support.js`) are present in `mock/` — confirm the founder's drop landed, or re-flag it in the PR body.
- [ ] Open the PR to `dev` (that **is** the promotion proposal — ADR-031's gate); never merge it unasked.
