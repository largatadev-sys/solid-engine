# 05: The contract doc and the tracker

**What to build:** the written contract matches the wire. The object contracts' per-route tables gain the twins so a reader of the trip's row finds both addresses and the thirteen routes that stay old-only with their reasons; the row for CM-1's retired ten-field read goes; the epic map's CM-3 line is annotated built; the BUILD_STATUS row goes done with its spec link, in the branch's last commit and nothing else.

**Blocked by:** 02 (The proof that the grammars cannot drift), 04 (The suites and scripts migrate).

**Status:** ready-for-agent

- [ ] The object contracts' wire table and per-route tables describe both roots and name the thirteen old-only routes with their reasons; no row describes the retired read
- [ ] The epic map's CM-3 line records built, with the date and anything the build changed against the spec
- [ ] The BUILD_STATUS row carries status and spec link only, updated in the last commit on the branch
- [ ] Anything the build surfaced that outlives the story is a line in the epic map's backlog, not a comment in code

## Comments
