# 05 — Deployed bucket, drivers, smoke, close-out

**What to build:** the story leaves the laptop — the deployed-dev bucket provisioned with the recorded conditional executed for real, the verification rungs closed across all four surfaces, and the story's bookkeeping done (spec decisions 6, 12; ACs 11–14).

**Blocked by:** 04 — activity photos + the gallery (every surface must exist to be verified).

**Status:** in-progress *(the deployed-bucket half waits for the owner — see comment 1)*

- [ ] The decision-6 conditional executed: the Railway bucket-region dropdown checked at creation — an Asia region offered → Railway Buckets; none → Cloudflare R2 with the `apac` hint (the recorded fallback, no new decision) — and the outcome appended to the spec's `## Comments`. *(Owner's — see comment 1.)*
- [ ] Deployed-dev env vars wired through the platform UI — no secret touches a committed file (the structural rule). *(Owner's — rides with the bucket.)*
- [x] The API smoke script covers upload/read/ladder across all four surfaces against the fresh local stack; the preview drive script covers avatar, cover, activity photos and the gallery — including a real web file upload via CDP file-chooser interception (deferred here from ticket 02, comment 5).
- [x] The surface sweep: every place that already receives `avatarUrl` on the wire but renders initials only — `AvatarStack`, the published creator byline — renders the photo with initials as the fallback (deferred here from ticket 02, comment 5).
- [ ] The emulator walk closes the device ACs end-to-end (avatar → cover → activity photos → gallery), on pool identities with roles stated (`t1 = owner, t2 = member` style — the self-identifying-fixtures rule). *(See comment 4 — needs a dev-build rebuild for the new native module.)*
- [x] AC 14 held: the whole story's diff carries zero entitlement code — no tier branch, no `can(`, greppable and clean.
- [x] BUILD_STATUS row updated — status + spec link, nothing else — in the last commit on the feature branch (never after the merge).
- [ ] Recorded here, executed at the promotion: the post-merge deployed-dev probe (upload + read against deployed dev, environment named, a discriminating signal stated before trusting it — the S1.1 rule). The S4.13 precedent: the probe belongs to the promotion, not the branch.

## Comments

**1 · The deployed bucket is the owner's, and it is the one decision left open.** Decision 6's conditional resolves in the Railway dashboard — the region dropdown at bucket creation is the only source of truth for a region list Railway does not publish. **Asia offered → Railway Buckets; none → Cloudflare R2 with the `apac` hint**, the recorded fallback that needs no new decision. Four env vars follow (`LARGATA_STORAGE_ENDPOINT/BUCKET/ACCESS_KEY/SECRET_KEY`), set in the platform UI, never in a file. Nothing in the code changes either way — that is what ADR-021's no-provider-hostname rule bought.

**2 · A REAL WEB BUG, found by driving the browser and not by any test — the S1.3 lesson repeating exactly.** `<Image>` on react-native-web **ignores `source.headers`**. The avatar uploaded successfully and then rendered as a broken image, because RNW issued a bare anonymous GET against a media endpoint that requires a bearer token. Every layer said green: Jest resolves the `.native` fork, the ITs never touch a browser, and the screen *rendered*. The tell was one line in the driver's request list — `ANON GET /v1/media/…` among `bearer` everything else. Fixed by platform-forking the resolver: native passes headers through, web fetches the bytes with auth via `apiClient.fetchBlob` and hands `<Image>` an object URL. Re-driven after the fix: **every media request now reads `bearer`.** Both halves are in CLAUDE.md's Gotchas.

**3 · Driving a web file picker took five attempts, and four plausible ones fail silently.** The picker creates a hidden input, clicks it, and removes it on settle; headless Chrome opens no chooser, so `cancel` fires immediately and the element is gone before CDP can reach it — `DOM.setFileInputFiles` then attaches to nothing and **reports success**. Rejected in order: querySelector-by-nodeId, resolve-by-objectId, `Page.setInterceptFileChooserDialog`, hooking `appendChild`. What works is planting the file *before* the click (patch `HTMLInputElement.prototype.click`, adopt a pre-seeded `DataTransfer`, dispatch `change`). `drive-preview.js` gains `--upload label=path`; the bytes come off disk, so the real handler → repository → multipart path runs.

**4 · The emulator walk is the one rung still open, and the reason is the JDK trap, not the code.** `expo-image-picker` is a **native module**, so unlike S4.13 (JS-only, walkable from Metro against an existing dev build) this story needs a real Gradle run to put the module on the device. CLAUDE.md's S4.12 gotcha applies directly: Android Studio's bundled JBR is now **Java 25**, so `withAndroidJdk.js` resolves, reports success, and points at the exact JDK it exists to exclude. A real JDK 21 plus `LARGATA_ANDROID_JAVA_HOME` is the documented fix. **The web preview covers the same flows end to end** (upload, serve-with-auth, render), so what the walk adds is the *native picker + native multipart* path specifically — worth doing, not a blocker for review.

**5 · The code review found a real defect the whole story had missed: EXIF orientation was never honoured.** Decision 5 and AC 5 both say a rotated phone photo must render upright, and ticket 02 checked the box — but `grep -i orientation` over the backend returned **zero hits**. The cause is subtle and worth recording: `ImageIngest` decoded to a `BufferedImage` first and handed *that* to Thumbnailator, and by then the EXIF tag is gone — pixels carry no orientation. So the library was in the tree for exactly one reason (honouring orientation, per its own dependency comment) and was being used in the one way that cannot. **The strip then made it permanent**, since the tag is discarded with everything else. Fixed by rotating from the *bytes* (`Thumbnails.of(InputStream).useExifOrientation(true)`) before decoding, and **sabotage-verified**: flipping the flag to `false` fails `aSidewaysPhonePhotoComesOutUpright` with a real EXIF `Orientation=6` fixture.

**6 · Other review findings, all actioned.** *Hard:* the one prose comment in the diff removed (CLAUDE.md's no-comments rule — replaced by a `neverUpscale` helper name); the three new `DELETE`s returned 200 + body against `05-api-conventions.md`'s unqualified *"Delete | DELETE | **204**, always"* — every other DELETE in the tree already returns 204, so mine conformed rather than starting an exception, and the three tests now verify the *effect* through a fresh read instead of trusting the delete's own body. *Judgement:* dead code deleted (`StorageSettings.isConfigured`, `PhotoService.of`, `deleteAllFor`, `apiClient.deleteReturning`); the three copies of the picker plumbing collapsed into `usePhotoAction`; `keepTheTripEvenIfItsCoverFailed` — correctly called "a comment wearing a function's clothes" — inlined; the misleading `PhotoTooLargeException` on a megapixel refusal split into `TooManyPixelsException` (a 60MP 2 MB PNG no longer claims a 10 MB limit). *Recorded, not fixed:* the duplicated photo-cap constant is now an epic-map line with the entitlement seam as its trigger — the failure is one-directional and mild, and the honest fix is a served config the codebase has no other need for. *Checked and dismissed:* the web blob cache cannot serve stale content, because every photo gets a fresh UUIDv7 and its storage key derives from that id.

**7 · Two test gaps the spec axis found, both closed.** AC 9 was only half-pinned — `CoverContractIT` checked `itinerary.cover_image_url` and `photo.storage_key` but nothing read `traveler.avatar_url` from the database; AC 8's freeze/fence assertions existed for the cover but not for activity photos. Both now have tests.

**8 · Verification, whole story.** Post-review totals: backend **137 unit + 507 integration**, all green (`AvatarContractIT` 9/9, `CoverContractIT` 9/9 sabotage-verified, `ActivityPhotoContractIT` 10/10, plus `S3ObjectStoreIT`, `PhotoStorageIT`, `PublishedProjectionIT` 19/19). Mobile **1755 tests / 54 suites**, `tsc` clean. Against the running stack: `smoke-media.js` **30/30** across all four surfaces. In the rebuilt preview container: a real file upload round-trips and every media read carries `bearer`. Three sabotage checks recorded: INV-11's strip, the cover audience ladder, and EXIF orientation.
