# 05 — Story gate

**What to build:** The full-stack close per the standing discipline — proof the four slices hold together on every rung before a promotion is proposed.

**Blocked by:** 01, 02, 03, 04.

**Status:** ready-for-agent

- [ ] Full mobile suite green; backend ITs green **by `Tests run:` counts, never the exit code**
- [ ] `smoke-all` run; the three-rung smoke closed (API + emulator + web preview)
- [ ] On the device: edit a Ready trip in place, edit an Active trip mid-lifecycle and post a diary entry immediately after — the demotion's old diary blackout is provably gone
- [ ] The two named mock deviations verified as ruled, not drifted: Step back on Ready; Edit Itinerary live on non-draft viewers
- [ ] BUILD_STATUS's S4.24 row flips to ✅ in the last commit on the feature branch
