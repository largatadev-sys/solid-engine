# CLAUDE.md — Largata standing rules

**Standing rules and pointers — not the execution workflow.** How stories are built (the loop, planning, review, slicing) is the workflow's job, chosen per project, deliberately not defined here. This file supplies the rules every workflow must obey. It is rules, not knowledge: if it's knowledge, it lives in the context package below and this file points at it.

---

## Context-package index

- Requirements brief → `docs/design/00-requirements-brief.md`
- Intent & constraints → `docs/design/01-intent-and-constraints.md`
- Domain model & invariants → `docs/design/02-domain-model.md`
- Tenancy / isolation → `docs/design/03-tenancy-model.md`
- Architecture & ADRs → `docs/design/04-architecture.md`
- API conventions → `docs/design/05-api-conventions.md`
- Engineering principles → `docs/design/06a-engineering-principles.md` *(ratified as-is)*
- Engineering decisions → `docs/design/06b-engineering-decisions.md`
- Epic map (the living backlog) → `docs/design/07-epic-map.md`

All code conforms to 06a + 06b. **Dial: MVP grade, except the ledger and the authorization guard, which run at Full rigor.** Name and justify any pattern used (P9).

**The design docs are a living baseline, not a frozen assessment** *(founder, 2026-07-17, at the UX-flow reconciliation)*. They record the initial thinking and hold as baseline — but the build will surface divergence, and when it does the docs change rather than rot: anything new (tech, requirement, concept, UX proposal) is **analyzed together first, then written into the docs** — into canon if decided, into a register/backlog entry attached to the story that will decide it if parked. Nothing load-bearing lives only in a conversation. The test for any change: does it improve what we are building.

## Agent skills

### Issue tracker

Local markdown, **tracked in git** — specs and tickets live under `docs/plans/<story-id>-<slug>/`; the epic map (`docs/design/07-epic-map.md`) remains the durable backlog. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles, used as-is. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context, mapped onto the existing `docs/design/` package. See `docs/agents/domain.md`.

## State — always maintain

BUILD_STATUS → `BUILD_STATUS.md`. Every story that lands updates its row — **status + spec link, and nothing else**: no SHAs, no branch names, no summary of what the story proved. The tickets carry the detail and git answers "where does it live"; anything duplicated in the tracker is a fact that rots. **Update the row before the merge, in the last commit on the feature branch** — updating after means committing straight to `dev`, which this workflow doesn't allow. Every change that wasn't a planned story gets a line in the off-epic ledger. Keeping BUILD_STATUS current is **non-negotiable** — a stale tracker misleads the next session with authority, which is worse than no tracker. Anything raised along the way (idea, issue, deferred feature) is captured in the epic map's backlog — that is its one home. Regression checklist → `REGRESSION_CHECKLIST.md` (a bug that escapes to a human adds a line; a recurring line graduates into the Maestro smoke suite).

## Hard rules (this codebase)

- **All workspace access goes through the authorization guard; never query workspace tables directly.** Workspace-scoped service methods take the resolved `Membership` as a parameter (Artifact 03). No inline authority checks anywhere.
- **Never inline a tier check.** No entitlement code exists yet — the seam is parked (ADR-009, amended 2026-07-28) — so today this rule means: write none, and do not improvise one. When it ships, all gating goes through `can(traveler, capability)`.
- **Every story spec records a one-line candidate-capability note** — the acts passing the potentially-gated test: a capability, not existing data · footprint-growing · not governance.
- **The ledger is append-only.** Corrections, waivers, settlements, reassignments are new Transfer entries — never UPDATE or DELETE on ledger rows (INV-8). Splits must sum to the expense total transactionally (INV-7).
- **API changes are additive only within /v1** — never rename, retype, remove, or change semantics of anything shipped (ADR-008; old app versions live for weeks).
- **Modules reference each other by ID + service interface only** — never another module's tables or internals (ADR-002).
- **Mobile: no raw fetch/API calls in UI code** — everything through the repository/local-cache layer's typed apiClient (ADR-001, P6).
- **Never log secrets, tokens, passwords, raw payloads, or PII.** Reference entities by ID (P3).
- **Where a mock exists, copy it — screen and icon both** *(founder ruling, 2026-07-31, at S4.9's fidelity pass)*. The archived mock set attached to a story is the **design baseline**, not an illustration: if a frame draws a cog wheel, ship a cog wheel and not the word "Details"; if the heading sits inside the body padding with no header bar, ship no header bar. **Read the mock's own markup for the answer** — S4.9's `<symbol>` defs supplied every icon path verbatim, and its two day frames settled where cost belongs by simply *differing*. The failure mode is silent and expensive: an approximation looks fine in isolation and only reads as wrong beside the frame it came from, so it survives every test and every self-review, and the founder pays for it a screenshot at a time. **Deviate only where the platform forces it** (a native spinner has no web equivalent) — and say so, rather than letting the gap pass as a choice.
- **No code comments — none.** No javadoc, no docstrings, no inline commentary, in source or in tests. Names and tests carry the meaning in code; the *why* lives in the spec, the ADR log, and the commit message (06b §10, P9 — *"recording why in the commit message, not a code comment"*). **A trap worth keeping becomes a test that fails when it is re-tripped**, or a Gotchas line here when no test can catch it (a filesystem or toolchain property) — never a comment, because a comment has no failure mode: change the code and it stays green while it starts lying. Tool directives (`eslint-disable*`, `@ts-expect-error`, `prettier-ignore`) are code that happens to use comment syntax, not commentary — keep the directive, drop any prose attached to it.

## Stop rules — ask the owner before doing

Touching the authorization guard or isolation semantics · schema migrations beyond the current story's additive tables · anything in auth/token handling · changing ledger semantics · changing entitlement/gating semantics · deleting data or writing destructive migrations · changing publish/visibility semantics · anything that breaks /v1 additivity · anything irreversible.

## Never commit secrets

No `.env` or any file containing credentials, keys, tokens, passwords, or **PII** enters a commit — ever. This is P3 (never-log-secrets) extended from logs to commits: secrets never enter *any* durable artifact. Two layers, no third-party tool required:

- **Structural guarantee (the real defence): `.gitignore` the secret files before the first commit.** `.env`, `.env.local`, anything holding credentials — gitignored so they *cannot* be staged. Real secrets live only in gitignored files locally and in the platform's env-var UI in prod; the repo carries `.env.example` with placeholders only. If nothing sensitive is ever tracked, there is nothing to leak — it does not depend on anyone remembering.
- **Backstop (soft): scan the staged diff before every commit.** Check the staged content for secret/PII patterns (`API_KEY=`, `SECRET`, `PASSWORD=`, tokens, long high-entropy strings, any `.env` in the staged files) and refuse to commit on a match. It is a backstop *because* it's an instruction — lead with the gitignore, never let the scan become an excuse to relax it.

A committed secret is compromised even if a later commit deletes it (it stays in history) — **rotate it.**

## Git workflow (promotion pipeline)

Branch→environment mapping: `main` → **production** (protected base, real data) · `preprod` → a **pristine copy of production** (the verify gate; data mirrors prod) · `dev` → **long-lived shared preview** (persistent; reseed at the developer's discretion, *not* ephemeral) · **local** → the **ephemeral full-stack instance** (fresh DB every redeploy) where feature branches are built and tested.

Flow: feature branches (`feature/<story-id>-<slug>`) are tested on the **local ephemeral instance**, then **squash-merged into `dev`**. Promotion: **cherry-pick** the verified changes from `dev` → `preprod`, verify against the prod-copy, then promote `preprod` → `main`. Commit convention: `feat(scope): <story-id> …` / `fix(scope): <story-id> …` — **the story id in the message is mandatory**: it is how commits are located (`git log --grep <story-id>`), since BUILD_STATUS stores no SHAs. *Commit messages carry **no agent signature** — no `Co-Authored-By`, no "Generated with" trailer, no attribution line; just the conventional message.*

**Promotions are propose-first.** The agent may run routine git directly — create the feature branch, commit to it — but it **proposes and waits for approval before any promotion**: squash-merging `feature → dev`, promoting `dev → preprod`, or `preprod → main`. Promotions cross environments and are hard to reverse; each is a checkpoint, not an autonomous step.

**The promotion unit is the epic, and each hop has its own mechanic** *(founder decision, 2026-07-17, at Epic 0's promotion — the first one this repo ever ran)*:

- **`dev → preprod` is a SQUASH**, one commit per promotion, named for what it ships (`feat(skeleton): Epic 0 — the walking skeleton, dev-verified`). **Why:** an epic is the shipping unit, so it is the rollback unit — when a deployment misbehaves you want *one* SHA to revert, not a boundary to locate under pressure. A partial revert of an epic is rarely coherent anyway (S0.6 without S0.5's tri-state does not build). The cost is accepted knowingly: **preprod/main carry no per-story history**, so `git log --grep <story-id>` works on **`dev` only** — which is where the granularity is wanted, for investigation rather than recovery.
- **`preprod → main` is a FAST-FORWARD.** Never squash or cherry-pick this hop: it would mint a *different* SHA for identical content, destroying the property the pipeline exists for — that **prod carries exactly what preprod verified**, checkable with `git rev-parse main preprod`.
- **Consequence, permanent:** after the first squash, `dev` and `preprod` share no commits. Later promotions are therefore squash-or-cherry-pick and **never** fast-forward — and this is exactly where the footgun above lives: a cherry-picked subset can leave a dependency behind, so prefer promoting the whole epic.

**Footgun to watch:** cherry-picking after squash-merges can pull a change whose dependency you didn't pick — a subset that built in `dev` may not build in `preprod`. Track inter-change dependencies before promoting.

**The promotion conflicts, always, and it is not a conflict — hit for real at Epic 1 (2026-07-29).** `git merge --squash dev` on `preprod` raised **127 add/add conflicts**, one for every file present on both branches. Nothing had actually diverged: after the first squash the branches share **no commits**, so their merge-base is `initial commit` and git sees each file as independently created on both sides. Resolving 127 files by hand would be hours of work and would silently invite exactly the mistake it looks like it is preventing. **The promotion means "preprod's tree becomes dev's tree", so say that directly:** `git read-tree --reset -u dev`, then commit with `preprod` as the parent. It is verifiable afterwards in a way a hand-merge is not — `git diff preprod dev` must come back **empty**. **Do the safety check *before* resolving, not after:** list what the promotion *removes* (`git diff --name-only --diff-filter=D preprod dev`) and trace each file to the story that replaced it. At Epic 1 that was two files, both legitimate (`OwnerMembershipResolver` → `RowBackedMembershipResolver` at S1.1; `itineraries/[id].tsx` → the `[id]/` directory at S1.3). A bulk resolution is safe only because that list was checked and short. **The mobile train is separate** (Artifact 04, ADR-010 — Android-only until the iOS activation): emulator/dev-build (local, non-EAS) → local backend · Play internal testing → preprod · Play release → prod. Release builds are made locally (Expo prebuild + Gradle) and uploaded manually.

## Verify on the local full stack — it is where feature branches are tested

**This is Artifact 04's environment topology (04 §83–89), restated here because S0.5 lost it:** feature branches are tested on the **full-stack local Docker instance** — `docker compose up` (Spring + Postgres 18 + MinIO), **fresh DB every redeploy** — and *then* squash-merged to `dev`. `dev` is the long-lived shared **preview**, not a test rung; testing a branch against it writes junk into the environment founders look at, and lets pre-existing data flatter a result that a fresh DB would have failed. Ephemerality is a testing tool, so it lives where you test (04 §85). The mobile side points at it the same way: **emulator / dev-build → local Docker backend** (04 §89).

**Scale the run to the stage of the work — the full stack is a gate, not a heartbeat** *(founder ruling, 2026-08-11, mid-S3.1 after several full-suite reruns during UI iteration)*. Re-running everything after each tweak buys nothing while the implementation is still moving: the answer is stale the moment the next edit lands, and it is slow enough to distort the session. Three stages, and the cost is deliberate at each:

- **While iterating** — the **affected Jest file(s) by path**, `tsc --noEmit`, and **Metro hot-reload on the device**. Seconds. **Do not rebuild the preview container to look at a change**: it bakes its JS at image build time, so every visual iteration costs ~4–6 minutes, and Metro serves the same JS live. S4.20 measured this and named it the session's real tax; S3.1 paid it again before the rule existed.
- **When a change is functionally done** — the walk for the **surface touched** (`drive-<surface>.js`), rebuilding the preview *now*, because that is when the container's baked bundle needs to match.
- **Before proposing a promotion** — the whole stack, **once**: full mobile suite, full backend ITs (counts read from the summary, never the exit code), and `smoke-all`.

**The exception that stays hard:** a change to genuinely **shared** code — a `src/media` helper, a hook, a design token, a repository method — still earns its broad sweep, but at the *end*, not per-edit. S3.1's `pickPhoto`/`usePhotoAction` edits are the case in point: the closing sweep surfaced 12 red walks, and proving they were pre-existing (a `dev` worktree, a junction, three runs) was work the story genuinely owed. Deleting an exported symbol is the same shape — run broad before you promote it.

Two rungs above it, each proving something the one below cannot:

- **The web preview is verified by building and running `mobile/Dockerfile.web-preview`**, never `expo export` + a static server *(S0.5, owner call — it found a real bug on its first run)*. Only the container exercises the **true build path** (`npm ci` from the lockfile, export inside the image) and the **true server** (Caddy + `Caddyfile.web-preview` — SPA `try_files`, cache headers, security headers). Anything else proves the bundle and nothing about what Railway serves; that gap hid a `Cache-Control` matcher that never fires for real URLs.
- **A sideload AC is the exception that proves the rule: it runs against the deployed `dev` backend on purpose** (S0.5) — the whole point is proving the *baked cloud URL*, which a local backend would defeat. Know which question the run is answering before choosing its backend.

The generalisation this repo keeps re-learning: **verify at the layer that ships, not the layer that is convenient.** Device ACs are closed on a device (S0.2's `getTokens()` was invisible to every green test); release-signed behaviour is proven on a release-signed build (S0.5's Google sign-in); server behaviour is proven on the real server.

## Running the local verification rig (the recipe, learned the hard way at S0.6)

**Every story that touches the mobile web surface or the native app runs this. It is written down because S0.6 rediscovered all of it from scratch — an hour of plumbing, none of it the story's actual work.** Ports are not arbitrary: the preview's port is *pinned* by two independent registrations, so change Metro's instead.

**The port map, and why it cannot be rearranged:**

| Port | What | Why it is fixed |
|---|---|---|
| 8080 | backend (docker compose) | `mobile/.env` and the container build arg point at it |
| **8081** | **preview container** | **Pinned twice**: the backend's dev CORS allowlist (`docker-compose.yml` `LARGATA_CORS_ALLOWED_ORIGINS` = 8081/8082 only) *and* the Google OAuth client's authorized JavaScript origins (console-registered — S0.6). Move the preview and Google refuses to render the button *and* the API preflights 403. |
| 8082 | Metro (`npx expo start --port 8082`) | The one that gives. Metro defaults to 8081 and *will* collide with the preview. |

```bash
# 1. Full stack (fresh DB every time — that is the point)
docker compose up -d --build
curl -s http://localhost:8080/v1/health          # {"status":"ok"}

# 2. Preview container — the TRUE build path (npm ci + export inside the image) and TRUE server
# `set -a; . ./.env; set +a` exports mobile/.env into the shell so the --build-arg values below
# resolve from it. Never paste the literals into the command: they land in shell history, and the
# transcript of whoever is watching. The file is gitignored; keep the values there.
cd mobile && set -a && . ./.env && set +a
docker build -f Dockerfile.web-preview \
  --build-arg EXPO_PUBLIC_API_BASE_URL="http://localhost:8080" \
  --build-arg EXPO_PUBLIC_FIREBASE_API_KEY="$EXPO_PUBLIC_FIREBASE_API_KEY" \
  --build-arg EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN="$EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN" \
  --build-arg EXPO_PUBLIC_FIREBASE_PROJECT_ID="$EXPO_PUBLIC_FIREBASE_PROJECT_ID" \
  --build-arg EXPO_PUBLIC_FIREBASE_APP_ID="$EXPO_PUBLIC_FIREBASE_APP_ID" \
  --build-arg EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID="$EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID" \
  -t largata-preview:local .
docker run -d --name largata-preview -p 8081:8080 -e PORT=8080 largata-preview:local

# 3. Native DEV build on the emulator (UI + flows; JS comes from Metro, debug-signed)
export ANDROID_HOME="$LOCALAPPDATA/Android/Sdk"      # REQUIRED — see gotcha below
export PATH="$ANDROID_HOME/platform-tools:$PATH"     # REQUIRED — else install fails after a green build
npx expo run:android                                 # or: gradlew app:assembleDebug + adb install
npx expo start --port 8082                           # NOT 8081 (preview owns it)

# 4. Native RELEASE build + sideload (the artifact that ships; JS bundled, real keystore)
#    Only for ACs that a debug build cannot close — see "which build proves what" below.
powershell -File mobile/scripts/build-release.ps1    # -ApiBaseUrl defaults to https://api-dev.largata.com
#   ^ prompts for the keystore password; echoes the baked API URL + signing cert in its summary.
#     Other params: -KeystorePath (default ~/keys/largata-release.keystore), -Alias (default 'largata')
adb install -r mobile/android/app/build/outputs/apk/release/*.apk
```

**Test identities: use the verified pool, and never invent `@largata.test` accounts again (S1.5).**

`@largata.test` is a **reserved TLD (RFC 2606)** — mail to it is undeliverable *by definition*, so accounts there can never be verified. Every story up to S1.5 minted them, and the cost stayed hidden until S1.5's smoke run: **`email_verified` gates S1.2's invite → accept, so that flow had never been exercised on any rung** — only in ITs with synthetic tokens. Not broken; unreachable by any harness.

The fix needs no code, no Gmail setting, and no backend change (all three verified at S1.5): **Gmail routes anything between `+` and `@` to the base mailbox, while Firebase treats each full address as a distinct account with its own UID.** One real inbox, unlimited genuinely-verifiable travelers. Our `@Email` validation accepts them and `normalize()` (strip + lowercase) preserves the `+`.

```bash
cd mobile && set -a && . ./.env && set +a
node scripts/test-pool.js list          # the pool + each member's verification state
node scripts/test-pool.js create        # create any missing + send their verification mail
node scripts/seed-trip.js --owner t1 --members t2,t3   # a trip seeded via the REAL invite→accept
```

- **Verification is a one-time human click, by design** — the thing being proven is mailbox control, which no script can fake. It only happens once: Firebase accounts live in `largata-dev`, so they survive every fresh-DB redeploy.
- `LARGATA_TEST_POOL_EMAIL_BASE` / `LARGATA_TEST_POOL_PASSWORD` live in the **gitignored `mobile/.env`** — a password is a credential and the address is PII, so neither enters a committed file (never-commit-secrets is structural, not a promise).
- **Stop planting `membership` rows with psql.** Fixtures did that only because unverifiable accounts could not pass the accept gate — which meant every fixture skipped the step that gate exists to protect. `seed-trip.js` drives invite → inbox → accept over HTTP instead.
- **Leave the pool's display names alone — a test identity must identify itself** *(founder ruling, 2026-07-27)*. The accounts have no `displayName`, so S1.2's fallback renders them as `largata.dev+t1`, and that is the point: in any check with two or more travelers the question you ask constantly is *"which fixture is this?"* — the owner or the member, the remover or the removed. A tag-derived name answers it on sight in a screenshot, a roster row, a log line or a DB row; a realistic name ("Ana Silva") forces a mapping held in your head, and a wrong mapping silently invalidates the observation. Realism buys nothing — nobody is reviewing the copy — while legibility buys the whole run. Same family as ADR-006's *identify projects by NUMBER, never name* and S1.1's *name the database in any query whose answer you intend to act on*. **State which tag played which role** in the write-up (`t1 = owner, t2 = removed member`) rather than inventing per-story identities.
- **Reach for the pool whenever a feature needs two or more travelers** — it is what makes multi-account verification cheap. S1.6 (ownership transfer), E2 (voting, one vote per member), and E5 (ledger splits) all need it; none should mint its own accounts.
- **Google sign-in is a separate axis the pool does not cover:** a Google sign-in always returns the *base* account, never a `+suffix`. Two Google-authenticated travelers need two real Google accounts. The AVD is already a Play image with the founder's dev Google account signed in (`com.android.vending` + `com.google.android.gms` both present — check with `adb shell pm list packages`), so regression-checklist line #4 is runnable on demand rather than needing setup.

**Which build proves what — do not spend a release build on a question a dev build answers.**

| | Dev build (`expo run:android`) | Release build (`build-release.ps1`) |
|---|---|---|
| Signed with | debug key (SHA-1 `5e8f1606…`) | the real keystore (SHA-1 `ff05c732…`) |
| JS from | Metro, live | bundled into the APK (Hermes bytecode) |
| Proves | UI, flows, native module wiring | **release-signed behaviour** — Google sign-in in particular, since it is the one thing that differs by signing key (S0.5) |
| Cost | ~1 min rebuild | full prebuild + Gradle + a password prompt |

A debug build works forever while the release one is broken — that is the S0.4 ninja trap and the S0.5 SHA-1 trap both. **Any AC about the shipping artifact is closed on a release build, on a device** (S0.5's rule); everything else runs on the dev build.

**The traps, each of which cost real time at S0.6:**

- **`adb` is not on the Git Bash PATH and `ANDROID_HOME` is unset.** Gradle fails with *"SDK location not found"*, which reads like a broken toolchain but is a missing shell variable. Do **not** "fix" it by writing `android/local.properties` — that tree is generated and gitignored (CNG), so the edit vanishes at the next prebuild. Export `ANDROID_HOME` instead. Separately, `expo run:android` will **compile successfully and then fail at install** if `adb` is not on PATH — the error is a `spawn ENOENT` deep in an ADBServer stack trace, nowhere near the word "adb".
- **Git Bash rewrites device paths.** `adb push x /data/local/tmp/y` becomes `C:/Program Files/Git/data/local/tmp/y` and fails with `secure_mkdirs() failed`. Prefix with `MSYS_NO_PATHCONV=1`.
- **`adb reverse` can be listed and inert.** `adb reverse --list` showed the tunnel while it carried zero traffic (the adb daemon had died — `adb kill-server && adb start-server` reveals it with "daemon not running"). Test the far end, never the plumbing: `adb shell "echo -e 'GET /status HTTP/1.1\r\nHost: localhost\r\nConnection: close\r\n\r\n' | nc localhost 8081 | head -2"`. If the tunnel is dead, point the app at Metro directly via `10.0.2.2` (the emulator's host-loopback alias) by pushing RN's dev pref:
  `<map><string name="debug_http_host">10.0.2.2:8082</string></map>` → `/data/data/com.largata.app/shared_prefs/com.largata.app_preferences.xml` (via `adb push` + `run-as … cp`).
- **`adb reverse` can be dead beyond reviving, and `10.0.2.2` is the fix for *both* Metro and the backend (S1.6).** The recorded remedy above — `adb kill-server && adb start-server` — reported *"daemon not running; starting now"* (the documented tell that it had died) and the tunnels **still** carried zero traffic afterwards, for Metro *and* the API. Don't keep re-establishing them: point everything at the emulator's host-loopback alias instead. `mobile/.env` already sets `EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:8080`, so only Metro needs the `debug_http_host` pref push — and prove the far end with `adb shell 'printf "GET /v1/health HTTP/1.1\r\nHost: 10.0.2.2\r\nConnection: close\r\n\r\n" | nc 10.0.2.2 8080'` (the device has `nc` but **no `curl`**). Note the pref push needs `MSYS_NO_PATHCONV=1` for the *device* path **and** `cygpath -w` for the *host* path — set only one and it fails in opposite directions, each with a misleading error.
- **A stale session on the emulator reads as a broken screen, and the backend log is the only honest witness (S1.6).** A leftover S1.5 sign-in (an `@largata.test` account, from before the verified pool) made the Members screen render *"Could not load members."* — which looks like a network or build failure and sends you to the toolchain. The server was answering `ITINERARY_NOT_FOUND` for an authenticated **non-member**: the guard masking correctly. Same family as S1.1's named-database rule — *check the discriminating signal, not the surface*. `adb shell pm clear com.largata.app` resets to the sign-in screen (and wipes the `debug_http_host` pref, so push it again after).
  - **…and the re-push must come *after* the app has been launched once, or it fails and you get a white screen (S1.7).** `pm clear` deletes the whole `shared_prefs` **directory**, not just its contents, so an immediate `run-as … cp` dies with *"No such file or directory"* — a one-line failure easy to miss in a scripted walk. The app then starts with no Metro host, renders **blank**, and every instinct says broken bundle or broken toolchain. The discriminating signal is one command, and it is not logcat: `adb shell "run-as com.largata.app ls -la /data/data/com.largata.app/shared_prefs/"` — an empty listing means the pref never landed. Correct order: `pm clear` → launch the app (the directory is created) → push the pref → `am force-stop` → launch again. Same family as the stale-session trap above: a blank screen is a *symptom* with several causes, so check the state the app actually has rather than diagnosing from the render.
- **`INSTALL_FAILED_UPDATE_INCOMPATIBLE`** means the emulator holds a build signed with a different key (e.g. a release APK from sideload testing). `adb uninstall com.largata.app` first — emulator state is disposable.
- **Metro serves `expo-router/entry.bundle`, not `index.bundle`.** Probing `/index.bundle` returns 404 and looks like a broken Metro; it is a wrong probe. Check `/node_modules/expo-router/entry.bundle?platform=android&dev=true` → 200.
- **`expo start` in non-interactive mode silently skips its dev server on a port clash** ("Port 8082 is being used… Input is required… Skipping dev server"). It exits *zero*, and `/status` still answers from the *older* instance — so the port looks healthy while nothing is bundling.
- **Evidence, not vibes.** Web: **`node mobile/scripts/drive-preview.js [url] [--shot out.png]`** — real headless Chrome, committed at S0.6 *because S0.5 wrote this driver, threw it away, and S0.6 wrote it again*. It reports the page text (empty = the S0.4 white screen), whether Google's iframe rendered, whether a One Tap overlay appeared, and every console/page error. Native: logcat showing `SignInHubActivity` → GMS `SignInActivity` (the real picker) plus a screenshot (`adb shell screencap -p /sdcard/x.png && adb pull …`). **Neither is sufficient alone:** a sign-in that reaches My Trips can still have failed — check `Traveler provisioned` in the backend log and the DB going 0 → 1. **Routing is not a round-trip** (S0.5).

## Google Cloud console: the OAuth client the preview needs (S0.6)

**The web preview's Google button needs an *authorized JavaScript origin* per browser origin, registered on the OAuth client — and this is the first story that reaches past Firebase's abstraction into the Cloud project underneath it.** S0.2–S0.5 never needed the Cloud console because Firebase managed the client invisibly. Landmines, all hit at S0.6:

- **A Firebase project may not appear in the Cloud console's project picker until you enter it once from Firebase** (Firebase console → gear → *view on Google Cloud*). Before that it looks like the project does not exist — and the console will happily offer to *create* one, appending a suffix because your real project already owns the name. **S0.6 configured a consent screen and an OAuth client in a phantom `largata-dev-502703` before noticing.** Nothing would ever have worked, with no error saying why: the origins simply would not apply. **Identify projects by NUMBER, never name — the real one is `309534715609` (`largata-dev`).** Same principle as ADR-006's environment identity: the name is a label, the number is the identity.
- **The client to edit is `309534715609-7jjk1jq73igts…`** — the `client_type: 3` (web) entry in `mobile/google-services.json`, and the value of `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`. The project contains ~5 auto-created clients (3 web, 2 Android); **edit only that one, delete none of the others.** The Android ones carry the SHA-1s that make native sign-in work; the other web ones are Firebase-managed. Editing a sibling leaves Google returning `400` for *our* client with no error saying "wrong client".
- **Registered origins (dev):** `https://founders.largata.com`, `http://localhost:8081`, `http://localhost`. Bare host and host:port are **distinct entries**. Redirect URIs stay empty — GIS's button flow delivers the credential to a JS callback and never redirects.
- **Propagation is 5 minutes to a few hours.** Until it lands, the button silently does not render — clean console, no page errors — which reads exactly like a code bug. **The only trustworthy check is whether Google's iframe is in the DOM:** run `node mobile/scripts/drive-preview.js` (committed at S0.6 — real headless Chrome, no new dependency) and read *"Google-rendered iframes: 1"*. **Ignore `/gsi/button`'s status code entirely** — S0.6 first built a `curl` watcher on it (reported 400 forever while the button worked), then found it returns **400 even from a real browser while rendering fine**. It is not the origin signal it appears to be, in any client. A check that contradicts the running system is worse than no check; twice burned by the same one.
- **Per project, not per app:** consent screen, OAuth clients, and origins are all per Cloud project — so `largata-prod` needs its own of each, and its own `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`. *(The prod-rung work itself is a backlog line — epic map, "preprod + prod environments".)*
- **Consent screen in "Testing" publishing status only admits accounts on its test-user list.** Add each founder's Gmail before a demo, or they hit access-denied and it reads as our bug.
- **`mobile/google-services.json` is a point-in-time snapshot.** It carries only the **debug** SHA-1 (`5e8f1606…`); the release key (`ff05c732…`, registered at S0.5) exists in the console but not in that file. The console is authoritative — do not diagnose from the JSON alone.

## Gotchas (grows during the build)

- **…and the JDK pin points at a PATH, not a VERSION, so Android Studio updating breaks it silently (S4.12, 2026-08-04).** `withAndroidJdk.js` pins `org.gradle.java.home` to Android Studio's bundled JBR because that JBR *was* Java 21. It is now **Java 25** — so the guard resolves, reports success, and points at the exact JDK it exists to exclude. The symptom is identical to the original trap below (`configureCMakeDebug` dying on *"WARNING: A restricted method in java.lang.System has been called"*), which makes it read as the known-and-fixed problem rather than a new one. **Check the version, never the path:** `"/c/Program Files/Android/Android Studio/jbr/bin/java" -version`. **This blocks release/APK builds only; it does not block testing**, because a dev build takes its JS from Metro, so JS-only changes are fully walkable on a device with no successful Gradle run at all. S4.12 conflated the two and wrongly reported mobile as unverifiable. *(Plugin hardening — assert the version rather than trust the path — is an epic-map backlog line.)*
  - **Corrected at S3.3 (2026-08-06): the requirement is "not 25", not "21", and the machine already had a usable JDK.** This entry's *"the fix is a real JDK 21 (`winget install …`)"* was inherited rather than tested, and it cost a wasted install attempt that stalled on a UAC prompt an agent shell cannot answer. What is actually true: **Gradle 9.3.1 runs happily on Java 25** (`gradlew --version` reports the daemon JVM cleanly) and only the **CMake/native step** fails — the build reaches `:react-native-worklets:configureCMakeDebug` before dying, so most of it compiles. **JDK 17 works**, and `C:\Program Files\Java\jdk-17` was already installed. `export LARGATA_ANDROID_JAVA_HOME="C:\Program Files\Java\jdk-17"` then `expo prebuild` writes the pin and `:app:assembleDebug` succeeds. **Before installing anything, enumerate what is already there:** `for p in "…/Android Studio/jbr" "/c/Program Files/Java"/* "$HOME/.jdks"/*; do "$p/bin/java.exe" -version; done` — that one loop would have skipped the whole detour.
- **THE TEST STACK'S VERSIONS, IN ONE PLACE — four different numbers, none of which is "the project's Java version" (settled at S3.3, 2026-08-07, after this was asked three times).** They are independent, and conflating any two produces an hour of confident wrong debugging:

  | Thing | Version | Who cares | Changing it means |
  |---|---|---|---|
  | **Backend runtime** | **Java 25** | `backend/pom.xml` — Spring Boot 4 requires it | A backend decision (S0.1) |
  | **Android *build* JDK** | **≤ 24** (JDK 17 here) | Gradle's daemon, via `withAndroidJdk.js` | Nothing about the app; only what compiles it |
  | **AVD / device** | **Android 16, API 36** | The emulator, running **ART (`libart.so`)** | A new system image |
  | **compileSdk / targetSdk** | **36** | AGP, must not exceed the device's API | An Android SDK decision |

  **The AVD contains no JDK and cannot "require" one** — it executes ART bytecode, so the desktop JDK is invisible to it. If a build works and the app misbehaves on the device, the JDK is never the explanation. The version the *device* constrains is `compileSdk`/`targetSdk` (36), which is the Android SDK — a different number that also says "version", which is exactly why the two get swapped. **Backend Java 25 and Android JDK 17 are both correct simultaneously; neither was ever "changed back".**

- **The Android build JDK is now self-healing — no env var, no manual step (S3.3).** `withAndroidJdk.js` used to pin Android Studio's bundled JBR **by path**, and when Android Studio updated that JBR from Java 21 to **25** the guard kept resolving, reported success, and handed Gradle the exact JDK it exists to exclude (the S4.12 finding). It now runs `java -version` on each candidate and takes the first **≤ 24**, so `expo prebuild` picks a working JDK with no `LARGATA_ANDROID_JAVA_HOME` set. The override still works and still wins. Two traps in writing that check, both of which make every JDK look absent: **`java -version` prints to STDERR**, so reading stdout finds nothing; and a hand-built `home + '\\bin\\java.exe'` path is mangled by shell escaping in any probe — use `path.join`.
- **A Gradle cache entry can survive a reboot corrupted, and the error names disk corruption rather than the reboot (S3.3).** `:app:checkDebugAarMetadata` failed with *"The contents of the immutable workspace … have been modified. The modification might have been caused by an external process, or could be the result of disk corruption"* — after an unplanned restart interrupted a transform mid-write. It reads like a broken machine; it is one stale directory. **Fix: `./gradlew --stop` (a live daemon holds a `.lock` and `rm` fails with *"Device or resource busy"*), then delete the named transform directory under `~/.gradle/caches/<gradle-version>/transforms/`.** Do not clear the whole cache — the message names the exact hash.
- **Two JDKs on one machine: backend = Java 25, Android = Java 21 — already fixed by `mobile/plugins/withAndroidJdk.js`, keep it that way.** The plugin pins `org.gradle.java.home` to Android Studio's bundled JBR at every prebuild (override: `LARGATA_ANDROID_JAVA_HOME`). **The wrong fix is editing `android/gradle.properties`** — generated and gitignored, so it vanishes at the next prebuild. **The symptom lies:** AGP rejecting JDK 25 dies at `configureCMakeDebug` printing only *"WARNING: A restricted method in java.lang.System has been called"* — a JDK warning that reads as a broken toolchain, never as a version mismatch. Hit at S0.2.
- **The Firebase JS SDK cannot be reliably bundled into an Expo web export — the web preview uses the Identity Toolkit REST API instead (S0.4), and going back to the SDK will re-break it.** Symptom: `Uncaught Error: Component auth has not been registered yet` at module load, blank page, clean network tab (so it sends you hunting in the wrong place). Cause: the SDK registers its auth component through an import *side effect* Metro tree-shakes out of the web bundle, so `getAuth`/`initializeAuth` have nothing to initialize. **Every documented Metro workaround was tried at S0.4 and none fixed the registration** — `unstable_enablePackageExports=false` (the top community answer, expo/expo#36588), steering `unstable_conditionNames` to `browser` (this *did* swap Firebase's RN build for the browser build — verified by the `getReactNativePersistence` marker going present→gone — and the error **persisted**), `initializeAuth` over `getAuth`, `.cjs` sourceExts. Each fixed a real layer without fixing the registration. **The pivot that works: import nothing from `firebase/*`.** `mobile/src/auth/firebaseWebRest.ts` signs in with `fetch` to `identitytoolkit.googleapis.com/.../accounts:signInWithPassword?key=<apiKey>` and refreshes at `securetoken.googleapis.com`; the tokens are ordinary Firebase ID tokens the backend already trusts (ADR-006 unchanged). **Native is unaffected** — it uses `@react-native-firebase` (a native SDK), and the `.native/.web` fork means only the web files ever imported `firebase/*`; there is no `metro.config.js` and the `firebase` npm dep is gone. If someone reintroduces the JS SDK for the preview, expect the white screen back. **Debugging lesson that cost several cycles: keep the loop at the *bundle* (grep the exported JS for the failing call sites), never at browser reloads** — the browser render is the slowest possible test and can't tell you *why*. Still true regardless of SDK-vs-REST: read `EXPO_PUBLIC_*` by direct static access (below), and `web.output:"single"` in `app.json` is required or the export emits no root `index.html` and a static host shows a directory listing, not the app.
- **Browser preflight to a *secured* endpoint needs CORS wired into the Spring Security chain, not the MVC layer — else it 401s with no CORS header and the browser blocks the real request.** A browser sends a preflight `OPTIONS` before any request carrying an `Authorization` header. That OPTIONS has no token, so if CORS lives in a `WebMvcConfigurer` (which runs *after* Spring Security) the chain 401s the preflight before any CORS header is added, and the real `GET` never fires — `Response to preflight request doesn't pass access control check`. Same family as the S0.2 filter-ordering gotcha. **Fix:** `DevCorsConfig` provides a `CorsConfigurationSource` **bean** (dev profile only), and `SecurityConfig` wires it with `.cors(Customizer.withDefaults())`, so Spring Security's own CORS filter answers the preflight `200` before auth. Prod/preprod run no profile → no bean → `.cors()` finds no source → no CORS (the invariant `ProdCorsAbsentIT` guards). **Invisible to native** (no CORS) and to any CORS test that only hits the *public* `/v1/health` — the regression test must preflight a *secured* endpoint (`DevCorsEnabledIT.preflightToASecuredEndpointIsAllowedBeforeAuth`). Hit at S0.4 the first time the web preview called a secured route.
- **Expo inlines `EXPO_PUBLIC_*` on web by literal text substitution of `process.env.EXPO_PUBLIC_X` — a computed `process.env[name]` is invisible to it and silently ships unresolved.** There is no runtime environment in a web bundle; Expo bakes these vars at export time by find-and-replacing the *exact source text* `process.env.EXPO_PUBLIC_FOO`. Read one indirectly — `const v = process.env[name]`, a `required(name)` helper, destructuring, anything non-literal — and the substitution pass skips it, the reference survives into the bundle, and it evaluates against an empty `process.env` in the browser: every value `undefined`, and on our `firebaseWebRest.ts` that means the REST `key=` param is empty and sign-in fails, **no error naming the cause**. Invisible to every test (Jest has a real `process.env`, so the helper works there) and to typecheck — it fails only in an actual web export, which is exactly the S0.2 `getTokens()` shape: green everywhere except the one environment that ships. **Rule: every `EXPO_PUBLIC_*` read must be a direct static member access.** Verify an export actually baked a value with `Select-String` for the literal in `dist/_expo/static/js/web/*.js` before trusting it. Also: Expo caches aggressively — use `expo export --clear` when a value ought to have changed and the bundle still shows the old one. Hit at S0.4 on the first web preview export.
- **The release build needs ninja ≥ 1.12 on Windows — already fixed by `mobile/plugins/withLongPathNinja.js`, keep it that way.** One-time setup per machine: `winget install Ninja-build.Ninja`; the plugin then injects `-DCMAKE_MAKE_PROGRAM` at every prebuild (override: `LARGATA_NINJA`). **Symptom:** `assembleRelease` dies with `Filename longer than 260 characters`. **Two wrong fixes, both plausible:** (1) enabling Windows long paths does nothing — `LongPathsEnabled` is already 1; *ninja* only learned them in 1.12, and Android SDK CMake bundles 1.10.2. (2) there is no `android.ninjaPath` gradle property — setting one is accepted, does nothing, and fails identically, because *CMake* chooses ninja, not AGP (the plugin's own header records this). Editing `android/gradle.properties` or `android/app/build.gradle` is never the fix — generated and gitignored. **Only release trips it**, so the toolchain looks healthy until the moment it must ship. Hit at S0.4.
- **`android/` and `ios/` are generated, not committed (CNG).** `expo prebuild` regenerates them from `app.json` + config plugins; anything hand-edited inside them is erased without warning. Native customization goes through a **config plugin** (`mobile/plugins/`), which is also why a fresh clone must run `npx expo prebuild` before it can build.
- **A fresh clone cannot prebuild without `mobile/google-services.json`** — `app.json` names it (`android.googleServicesFile`), the RNFirebase plugin hard-fails without it, and it is gitignored by design (it holds an API key; grilling Q4). Download it from the Firebase console → `largata-dev` → Project settings → the `com.largata.app` Android app. The failure is an error about a *config field*, which does not obviously mean "download a file". CI is unaffected: the mobile job never prebuilds.
- **Google sign-in: pass BOTH tokens to `GoogleAuthProvider.credential(idToken, accessToken)`, via `GoogleSignin.getTokens()`.** RNFirebase v25's *JS* layer says an idToken alone is fine (it only rejects when both are null) but its *native* layer then throws `[auth/unknown] Exception in HostFunction: accessToken cannot be empty` — **the two layers disagree, so the TypeScript signature lies**. `signIn()` returns only the idToken; `getTokens()` returns both. Hit at S0.2 and invisible to every unit test: the mock returned an idToken and the JS layer accepted it, so the failure existed only on a device. The Jest mock now enforces the *native* contract, not the JS one.
- **To reach a screen the UI has no button for, deep-link it: `adb shell am start -a android.intent.action.VIEW -d "largata://<route>"`.** The scheme is **`largata`** (`app.json` → `scheme`), *not* the applicationId `com.largata.app` — guessing the latter yields "unable to resolve Intent", which reads as "deep-linking isn't wired" when it is. This runs against the app's **live session**, so it exercises any route as the signed-in traveler with no debugger, no Metro console, and — the point — no digging credentials out of app-private storage. It is how S0.3's guard AC was closed: traveler B could not reach traveler A's itinerary by tapping (correctly, it is not in B's list), but `largata://itineraries/<A's id>` asked for it directly and got the 404.
- Share-sheet capture requires an Expo **dev build** — plain Expo Go cannot load the native share extension (ADR-004).
- Local **iOS builds require macOS + Xcode** — Android builds run anywhere via Gradle. **The recorded answer is EAS at the iOS activation** (or earlier, if Android release cadence makes manual builds a cost) — no Mac purchase planned (ADR-010).
- **`expo install --check` reporting `react@19.2.7 - expected version: 19.2.3` is expected — do not "fix" it.** Two Expo-published constraints contradict each other (SDK 57 pins `19.2.3`; `expo-router` pulls `react-dom@19.2.7`, needing `react@^19.2.7`). We hold `19.2.7` — the value that resolves without `--legacy-peer-deps`. *(Re-check at the next SDK bump: backlog line, epic map.)*
  - **Corollary: `react` and `react-dom` must be pinned to the same exact version, and `npx expo install react-dom` breaks that** — it installs the SDK's `19.2.3` against our `react@19.2.7`, and the web bundle then dies at runtime with *"Incompatible React versions"* on a white screen. After any `expo install` that touches React, check both versions match. (Hit at S0.1 when adding web support.)
- **`<Image>` IGNORES `source.headers` on BOTH platforms, so any authenticated image silently fails while every test stays green (S3.3).** The documented `{uri, headers}` shape is a lie in react-native-web *and* in RN 0.86 on Android: both issue a bare anonymous GET. Our media endpoint requires a bearer token, so an uploaded avatar rendered broken — *after* a successful upload, which is what makes it read as a server bug rather than a client one. **Invisible to every test**: Jest resolves whichever fork it likes, the ITs never touch a client, and a driver that only checks screens render sees a screen that renders. **The two tells, and neither is the screen:** the driver's API-request list showing `ANON GET /v1/media/…` among `bearer` lines, and the backend logging `Security rejection: code=UNAUTHENTICATED` for a GET the app just made. **Fix: never hand `<Image>` a remote URL for our own media.** `mediaSource.web.ts` fetches the bytes through `apiClient.fetchBlob` (auth attached) and hands back an object URL; `mediaSource.native.ts` uses `File.downloadFileAsync(url, dest, {headers})` and hands back a `file://` uri. Both memoise per media path — safe because a photo id is UUIDv7 and its storage key derives from it, so a replaced photo is always a new URL. Same family as the `Alert.alert` trap below: an RN API whose implementation quietly does less than its types promise.
- **React Native 0.86 rejects the `{uri, name, type}` FormData idiom, and its Blob cannot be built from an ArrayBuffer — so a native multipart upload needs `expo-file-system` (S3.3).** The decade-old RN recipe now throws **`Unsupported FormDataPart implementation`**, and the obvious fallback (`await (await fetch(fileUri)).blob()`) throws **`Creating blobs from 'ArrayBuffer' and 'ArrayBufferView' are not supported`**. Both errors are swallowed by any `catch` that maps failures to an offline message — ours said *"Could not reach the server"* for an error that never touched the network, which sent the investigation to connectivity and the emulator's `10.0.2.2` tunnel. **The fix is `expo-file-system`'s `File`, which `implements Blob`:** `part.append('photo', new File(photo.uri), photo.name)`. **Diagnostic lesson worth more than the fix:** when a catch-all maps every failure to one message, temporarily log the raw cause — one `console.warn` turned an hour of network debugging into a one-line answer.
- **`expo-image-picker`'s crop options degrade silently off Android — `shape` and `aspect` are Android-only, and the web fork ignores them entirely (S3.3).** `shape: 'oval'` gives the avatar its circular crop overlay on Android (it maps to CanHub's `CropShape.OVAL`); on **iOS the crop rectangle is always square** and the option does nothing; on **web** `ExponentImagePicker.web.ts` builds a bare `<input type=file>` and never reads `allowsEditing`, `aspect` or `shape` at all. No error, no warning — the same silent-degradation family as `Alert.alert` and `<Image>`'s headers. **The defence is server-side and already built:** ingest centre-squares anything stored as `TRAVELER_AVATAR` (`ImageIngest.Framing.SQUARE`), so an avatar is square whatever the client managed to crop, and the rendered circle never cuts off something the traveler framed. Keep that invariant if the picker is ever swapped — a client-side-only crop guarantee is one platform away from being false.
- **A web file picker is undrivable by a post-click DOM query, and the failure looks like a broken button (S3.3).** `pickPhoto.web.ts` creates a hidden `<input type=file>`, clicks it, and removes it on settle. In headless Chrome no chooser can open, so `cancel` fires **immediately** and the element is gone before any CDP `DOM.querySelector` reaches it — `DOM.setFileInputFiles` then attaches to nothing and reports success. Four plausible fixes fail in sequence (querySelector by nodeId, resolve by objectId, `Page.setInterceptFileChooserDialog`, hooking `appendChild`). **What works: plant the file before the click** — patch `HTMLInputElement.prototype.click` so a file-type input adopts a pre-seeded `DataTransfer.files` and dispatches `change` itself (`drive-preview.js --upload label=path`). The bytes are read from disk, so the real change-handler → repository → multipart path runs exactly as a traveler's does.
- **`Alert.alert` is a NO-OP on react-native-web — every RN API that fronts a native dialog needs a web fork, and the failure is total silence.** react-native-web's `Alert` is literally `class Alert { static alert() {} }`. Any control whose whole job is to say something — a confirm, a "coming soon", an error the traveler must see — therefore does **nothing at all** in the browser while working perfectly on the device. S1.3 shipped its entire grey-out shell (four nav tabs, two media tiles, the Preview CTA) as dead clicks on the web this way, violating the AC that *defined* the ticket. **Invisible to everything:** the unit test asserted the `Alert` call and passed (Jest resolves the `.native` fork), the ITs never touch the client, and a preview driver that only checks screens *render* sees nothing wrong — "renders on web" is not "works on web". Found by a human tapping the preview. **Fix:** platform-fork the helper (`x.native.ts` / `x.web.ts`, the auth and date-picker pattern), keep the shared wording in a third module so it cannot drift, and verify by *driving* the container — intercept `window.alert` in CDP, since headless Chrome swallows native dialogs. The same suspicion applies to any other RN API whose web implementation is a stub. Hit at S1.3.
- **Expo Go is abandoned — use the dev build (`npm run android`), do not go back.** Resolved at S0.2 on **two** independent blockers, so fixing either one changes nothing: the store client lagged SDK 57 (*"Project is incompatible…"*), and the CDN-sideloaded SDK 57 client — which bypasses the store, disproving the lag theory — **segfaults natively** (`Fatal signal 11 (SIGSEGV), tid mqt_v_js`) after our bundle loads. Neither is ours to fix.
- **Testcontainers 2.x renamed everything** — artifacts (`postgresql` → `testcontainers-postgresql`) *and* packages (`org.testcontainers.containers` → `org.testcontainers.postgresql`; the old package is a deprecated shim, and the container class is no longer generic). The 1.x coordinates 404 at 2.x, which reads like "2.x is broken" — it isn't. Versions come from the Boot BOM; never hand-pin them.
- **Boot 4 moved Flyway's auto-configuration out of the JPA starter** into `spring-boot-starter-flyway`. With only `flyway-core` on the classpath, Flyway silently never runs — the app boots green and no migration applies. Use the starter.
- **Boot 4.1 ships Jackson 3: `ObjectMapper` is `tools.jackson.databind.ObjectMapper`, not `com.fasterxml.jackson.databind`.** Only `jackson-annotations` still lives under `com.fasterxml`. Every Jackson-2-era example (i.e. the entire internet's Spring corpus) fails to resolve; write methods also no longer declare `IOException`. Hit at S0.2 in the auth entry point.
- **Boot 4 split autoconfiguration into per-technology modules, and the packages moved with them** — e.g. `OAuth2ResourceServerProperties` is now in `org.springframework.boot.security.oauth2.server.resource.autoconfigure`, not `org.springframework.boot.autoconfigure.security.oauth2.resource`. When an autoconfigure import won't resolve, the class almost certainly still exists under a new path: find it (`jar tf` the module) rather than assuming it's gone. Same family as the Flyway-starter gotcha above.
- **An unordered `@Component` filter runs *behind* Spring Security (chain order -100), not in front of it.** Boot gives unordered filters `LOWEST_PRECEDENCE`, so anything a rejected request needs — a `traceId` in the MDC, say — is never set, and the 401 renders without it. Invisible on the happy path (a 200 gets it either way). Any filter that must cover auth failures needs an explicit `@Order(HIGHEST_PRECEDENCE)`. Conversely, a filter that needs the *principal* must be registered inside the chain (`addFilterAfter(...)`) — as a `@Component` it runs too early and silently does nothing. Both hit at S0.2.
- **Spring Security's resource-server DSL installs its own `AuthenticationEntryPoint`**, so setting one on `exceptionHandling()` alone only covers "no credentials at all" — a *bad* token (expired/forged/malformed) answers through `BearerTokenAuthenticationEntryPoint` instead, with an empty body. One situation, two response shapes. Set it on `oauth2ResourceServer(...)` **and** `exceptionHandling(...)`. Hit at S0.2; caught only because the AC tested four token flavors rather than one.
- **`@Transactional` on a method called from within the same bean does nothing** (self-invocation bypasses the proxy). And a constraint violation marks its transaction rollback-only, so "catch the violation, then re-read the winner's row" fails *in the same transaction* — the recovery read dies too. Insert-on-conflict recovery therefore needs the insert in a **separate bean** with `REQUIRES_NEW` (and `saveAndFlush`, so the violation is raised inside the try, not deferred to commit). Hit at S0.2 in Traveler provisioning.
- **Testcontainers hangs forever at `Testcontainers version: 2.0.5` — and Docker is not the problem.** No container starts, nothing times out, while `docker ps`, `docker version` and even `docker run postgres:17-alpine` all answer in milliseconds. That contradiction is the tell: **`jstack` the surefire fork** (find its pid via `Get-CimInstance Win32_Process -Filter "Name='java.exe'"`, look for `surefire` in the CommandLine) and the `main` thread is parked in `docker-java`'s `HijackingHttpRequestExecutor`, waiting on a response header the daemon never sends. Ryuk — Testcontainers' reaper — opens a *hijacked* HTTP connection over the Windows named pipe and that path stalls; the CLI never hijacks, which is why it looks healthy. Workaround: `TESTCONTAINERS_RYUK_DISABLED=true` (Ryuk only reaps leftover containers; without it, clean them by hand if a run dies). **Kill only your own JVMs, by pid** — never `taskkill /IM java.exe`, which also takes out the IDE's language server, SonarLint and Android Studio. Hit repeatedly at S0.3; intermittent (the same suite passed once at 311s before wedging on every later attempt).
- **A *second*, different Testcontainers wedge on Windows: the Docker credential helper hangs — and the image already being present locally is what makes it baffling.** Symptom identical to the Ryuk one (no container in `docker ps`, no timeout, the fork alive), but `jstack`ing the surefire fork shows `main` parked in **`RegistryAuthLocator.runCredentialProgram → ProcessExecutor.waitFor`**, not `HijackingHttpRequestExecutor`. Before using even a *locally cached* image, Testcontainers resolves registry auth by shelling out to the credential helper named in `~/.docker/config.json` (`"credsStore": "desktop"` → `docker-credential-desktop.exe`), and that subprocess never returns here — so `postgres:18-alpine` is sitting in `docker images` while the run hangs on an auth lookup it does not even need. `TESTCONTAINERS_RYUK_DISABLED=true` does **not** fix this one (different code path). **Workaround: point `DOCKER_CONFIG` at a dir holding a minimal `{}` `config.json`** (no `credsStore`), so Testcontainers finds no credential program to run: `mkdir cfg && echo '{}' > cfg/config.json && DOCKER_CONFIG=$(pwd)/cfg mvn …`. Then the container starts in seconds and the migration/tests proceed. Hit at S1.2; the two wedges can masquerade as each other, so **`jstack` and read the actual parked frame before picking a fix** — Ryuk-disable vs. DOCKER_CONFIG are not interchangeable.
- **A query against a deployed rung must name the database it belongs to — "no rows" from the wrong one is indistinguishable from "the answer is no".** Deployed dev's backend connects to **`postgres.railway.internal:5432/railway`** — database **`railway`**, a name nothing in this repo would let you guess (it is Railway's default, not ours). At S1.1 the post-deploy check was handed over as bare SQL, returned zero rows, and was read by agent *and* owner as *"the migrations never ran"* — sending the investigation to CI and `gh auth` while the deploy log showed both migrations had applied cleanly minutes earlier. The null result was ambiguous and nobody noticed. **Name the database (and the environment) in any query whose answer you intend to act on**; same family as ADR-006's environment identity and S0.6's phantom Cloud project — *the thing you are talking to must be identified, never assumed*.
- **A check whose two outcomes are indistinguishable is not a check — and this repo has now been burned by one three times.** S1.1's version: to detect whether a deploy had landed, the agent created an itinerary on deployed dev and viewed it, reasoning only the new build could serve the view. Both builds return 200 (old: `owner_id`; new: a membership row) — same response, opposite mechanisms. It happened to hit the new build, so it gave the *right answer by an invalid method*, which is worse than a wrong one: it would have "confirmed" the deploy just as happily before it happened. Precedents: S0.6's `/gsi/button` watcher (400 forever while the button worked) and its `curl`-vs-browser cousin. **Before trusting a probe, state what its failure looks like.** If you cannot, it has no failure mode and proves nothing. The signals that *were* discriminating here — `flyway_schema_history` and the deploy log — were both outside the API.
- **A data migration is invisible to every test surface this repo owns — it runs against zero rows and "passes" as a no-op.** The local stack is fresh-DB-every-redeploy, Testcontainers boots empty schemas, CI likewise: on all of them a backfill does nothing and reports success, so the suite is green whether the SQL is right, subtly wrong, or a typo. The only database holding the rows it targets is the deployed rung. **The fix is a migration-stepping IT** (`WorkspaceBackfillIT` is the pattern): its *own* container — never `PostgresTestBase`'s singleton, which is shared by the run and already migrated to head before the class loads — Flyway `.target(V(n-1)).migrate()`, seed the legacy shape via **raw SQL** (going through the service would create the very rows the fixture must lack), then `.target(V(n)).migrate()` and assert. **And check the test can fail:** S1.1 sabotaged V5's timestamps to `now()` and confirmed the test caught it with the right diagnosis. A migration test that passes against a no-op is worse than none. Pair it with a post-merge check on the rung that has the data — the shape rehearses locally by planting a legacy row in the running stack and applying the migration file itself through psql (S1.1 did: owner 404 → apply → 200). Same family as S0.2's `getTokens()`: green everywhere except the one environment that ships. Hit at S1.1, on the first data migration this repo ever wrote.
  - **…and the sabotage itself runs against the LAST BUILD's SQL unless you recompile, so it "passes" and you conclude the test is worthless (S4.13).** `mvn failsafe:integration-test` does not run `process-resources`, so an edited `.sql` under `src/main/resources` is **never copied to `target/classes`** and Flyway loads the *previous* version. S4.13 sabotaged V21's remap, watched all 8 assertions stay green, and was one step from recording "this migration test has no failure mode" — the exact false conclusion the rule above exists to prevent, reached by trusting the tooling instead of the test. Adding `test-compile` to the goal list made the same sabotage fail 4 assertions instantly. **Always `mvn -o test-compile failsafe:integration-test …` when what you changed is a resource, and treat a sabotage that passes as a suspect *invocation* before a suspect *test*.** No test can catch this — it is a property of the build lifecycle, not of any program (06b §10's escape hatch, same category as the case-collision trap).
- **`@Enumerated(STRING)` writes the enum's NAME (`OWNER`), and any SQL that tests that value is a silent contract with Hibernate.** V3's `state TEXT DEFAULT 'draft'` is dead weight — Hibernate overrides it with `DRAFT` on every write — and that lower-case spelling is a trap for the next migration that copies it. S1.1's first draft of V4 wrote `CREATE UNIQUE INDEX … WHERE role = 'owner'` to enforce INV-4: the predicate would have matched **zero rows**, so the index would create successfully, cost nothing, and enforce **nothing** — the invariant unguarded, no error, ever, and no other test would have noticed (a partial index on a never-matching predicate is the quietest possible failure). Caught before it landed; `MembershipStorageIT` now pins the spelling. **The distinction to keep:** an enum's *storage* spelling is a contract between Hibernate and SQL; the API's spelling (`wireName()`) is a *different* contract that happens to look similar. Any constraint, index or `WHERE` naming an enum value needs a test that would fail if the spelling moved.
- **A Spring `@MockitoSpyBean` wraps the `@Transactional` proxy, so `when(spy).method(…)` runs the transaction interceptor during *stub setup*.** With `Propagation.MANDATORY` on the method, stubbing throws `IllegalTransactionStateException` before the test body starts — surfacing as a Mockito `InvalidUseOfMatchersException` about misplaced matchers, which names nothing about the real cause and sends you hunting in the test's syntax. Stub one layer lower (the repository the service saves through) — no AOP proxy in the way, and the failure is injected where a real one would happen. Hit at S1.1.
- **Testcontainers: use the singleton-container pattern (`PostgresTestBase`), never `@Testcontainers`/`@Container` on a shared base.** Those annotations manage the container *per test class*; Spring caches contexts *by configuration*. The moment a second context exists (e.g. a test with a different `@ActiveProfiles`), a cached context keeps a datasource pointing at a container JUnit already stopped. **Signature: 30-second JDBC timeouts and 503s in test classes you did not touch**, right after adding a test with a different profile. Hit for real at S0.1 when the CORS profile tests landed.
- **Failsafe matches `*IT` on the *outer* class only** — integration tests nested as `static` inner classes are silently skipped, which looks exactly like passing. One test class per file. (Also hit at S0.1: a CORS test "passed" without ever running.)
- **A test that needs a credential-*shaped* string trips the pre-commit hook, and widening the hook is the wrong fix.** `FirebaseAdminSdkBootTest` generates a throwaway RSA key at runtime, so the PEM header is the only literal — but rule 3 of `.githooks/pre-commit` is deliberately exemption-free (*"nobody writes a real-looking PEM header as an example"*), and relaxing a backstop for one test file is exactly the erosion CLAUDE.md's never-commit-secrets section warns against. **Assemble the boundary instead** (`"-".repeat(5) + label + …`) so no header literal ever enters the file; the method name carries the why. Same answer for any future fixture needing an AWS/Stripe/JWT-shaped string. Hit at S4.0.
- **A dependency can change the HTTP transport under the whole application, because Spring picks its `ClientHttpRequestFactory` by *classpath detection* — and the first symptom names nothing.** Adding `firebase-admin` (S4.0) put Apache HttpClient 5 on the classpath transitively. Spring then switched every `RestClient` and `RestTestClient` onto it, and HttpClient 5's default retry strategy **retries 503** — so an untouched test (`HealthUnavailableIT`) began seeing one outage logged twice and took 3x as long, while a Resend 503 would have silently retried in production. **The obvious fix is wrong and looks right:** excluding `httpclient5` compiles, keeps every unit test green, and then kills the app at startup with `NoClassDefFoundError: org/apache/hc/client5/http/nio/AsyncClientConnectionManager` from `FirebaseOptions`' constructor, because `ApiClientUtils.getDefaultTransport()` probes for the HTTP/2 transport unguarded. **Keep the dependency; state the transport where you own the client** (`ResendMailTransport.statedTransport()`, and `RestTestClient.bindToServer(new JdkClientHttpRequestFactory())` in the ITs that care). `FirebaseAdminSdkBootTest` reproduces the startup failure in under a second so nobody has to rediscover it with `docker compose up`. **Diagnosing it needed a clean worktree at HEAD** — same test, green there and failing here, which ruled out "pre-existing flake" in one run.
- **…and the Admin SDK's *default* transport answers 503 for a call that SUCCEEDED.** With `getDefaultTransport()` left to choose, `updateUser` flips `email_verified` on Google's side and then dies decoding the response — `FirebaseAuthException: Unknown error while making a remote service call: Not in GZIP format` — so the API returns 503 while the traveler is, in fact, verified. On the OTP screen that reads as "the code never works", forever, for an account that is already fine. **Set the transport explicitly**: `FirebaseOptions.builder().setHttpTransport(new NetHttpTransport())`. **Nothing below a real round-trip against real Google can see this** — every IT stubs `EmailVerificationFlag`, which is exactly what the seam is for, and exactly why the seam is not a substitute for the walk. Hit at S4.0.
- **Every Spring context in the test suite holds its own connection pool, and Postgres runs out at roughly ten contexts.** Adding ITs with new configurations (a `@MockitoBean`, a different `@TestConfiguration`) pushes the singleton container past `max_connections=100`, and the failure is `FATAL: sorry, too many clients already` in **test classes you did not touch**, plus 60-second timeouts that make unrelated assertions flap. `PostgresTestBase` caps `spring.datasource.hikari.maximum-pool-size`; keep it capped, and lower the cap rather than raising the container's limit when contexts multiply again. Hit at S4.0.
- **The preview driver clicks the screen *underneath* unless it prefers the visible match.** expo-router keeps previously-visited screens MOUNTED beneath the current one, so `querySelectorAll(...).find(...)` returns the *old* screen's control. `drive-preview.js` reported `click Create Account -> ok` while nothing happened — no console error, no page error, no network request — which reads as a broken button and sends you into the component. Every lookup now takes the **last visible** match and reports how many it saw. Hit at S4.0.
- **Back navigation is a property of the navigator tree, not of any call site — if back lands on the wrong screen, the stack is wrong, and no `push`/`navigate`/`replace`/`backTo` can rescue it (S4.13).** Sibling routes under a `Tabs` navigator are **separate stacks that share no history**. Trips lived at `(tabs)/index.tsx` while `itineraries/`, `members/` and `published/` were sibling tabs, each its own `Stack` — so the trip stack accumulated every trip ever opened and **never contained Trips**. Back therefore popped into a *previous trip's* screen, which reads as a wiring bug at whichever button you pressed last. **Five call-site fixes were tried and all reverted** (`navigate` over `push`; an `alwaysBackTo` override; a hardware-back handler; a `from` param; `dismissAll()`, which errors with *"POP_TO_TOP was not handled"* because it is Stack-only and the nearest navigator was Tabs) — each plausible, each impossible, since **a stack cannot pop onto a screen it does not contain**. The fix is one `(trips)` route group rooted at Trips, holding all four; it **removed 45 lines net** because every `backTo` guess and the `from` param became dead weight. **Parenthesised segments do not appear in URLs**, so `/itineraries/[id]/days` and `/published/[id]` were unchanged and not one `pathname` string moved — `tsc` confirms this in one run. Diagnose with the question *"which stack is this screen in, and what is its root?"* before reading any handler.
- **A component and its helper cannot differ only by case — the import silently resolves to whichever the filesystem saw first.** `LifecycleBanner.tsx` beside `lifecycleBanner.ts` collides on Windows and macOS (case-insensitive filesystems), and TypeScript resolves the import to one of them arbitrarily: the screen imported the *helper* and failed with *"only refers to a type"* — an error that names a type problem, not a filename problem. **Fix by naming, not by casing:** the component is `TripLifecycleBanner.tsx`, distinct from `lifecycleBanner.ts` by more than a capital letter. Matching case is not a fix — it regresses the moment someone renames on a case-sensitive box (CI is Linux) and it works there. **This is the one trap in the tree with no possible test** (06b §10's escape hatch): it is a property of the filesystem and the resolver, not of any program, so nothing Jest or `tsc` runs on this machine can catch it. Hit at S1.7.
- **`mvn test` runs NO integration tests, and there is no `./mvnw` in this repo — so the obvious backend command is a check with no failure mode (S4.19).** Two traps that compound. First: the `*IT` classes are **failsafe**-bound, so `mvn test` (surefire) skips every one of them and exits 0; add `-q` and there is no summary to contradict you. The suite that actually proves the server is `mvn -o test-compile failsafe:integration-test` (**545 tests** at S4.19), and `test-compile` is not optional — without it an edited resource never reaches `target/classes` (the S4.13 lesson above). Second: **`backend/` has no Maven wrapper.** `./mvnw …` fails with *"No such file or directory"* — and when it is the first command in a pipeline ending in `| tail`, the shell reports **exit 0**, so a backgrounded run "completes successfully" having done nothing at all. Maven lives at `/c/apache-maven-3.9.12/bin/mvn`. **Read the `Tests run:` counts, never the exit code** — S4.19 ticked its backend box twice on exit 0 before either run had executed a single IT.
- **Metro's LogBox warning banner eats taps on the bottom docked CTA, and the button looks simply dead (S4.19).** A dev build shows *"Open debugger to view warnings."* pinned to the bottom of the screen, and it sits **over** any docked action rail — so `input tap` on a docked submit button (Create Trip, Save) lands on the banner instead. Nothing indicates this: `uiautomator dump` reports the button's bounds correctly, the tap returns success, the screen does not change, no request reaches the backend, and no validation error renders. Every symptom says "the submit handler is broken", which sent S4.19 to re-check the form, the validator and the mutation before the banner was noticed in a screenshot. **Fix: dismiss the banner (`input tap` its ✕, top-right of the banner) before tapping anything in the bottom rail.** Same family as the blind-tap-chain trap below — the answer is always *screenshot first, then tap*, because the thing intercepting the tap is invisible to every programmatic probe. Note also that `KEYCODE_BACK` to dismiss the keyboard **navigates the router** if no keyboard is up, silently abandoning a filled form; use the keyboard's own collapse chevron instead.
- **A blind adb tap-chain will eventually press a destructive button, because the app cold-starts into the LAST route, not Home (S3.3).** `am start` after a force-stop restores wherever the traveler last was (expo-router persists the route), so a scripted walk that assumes it starts at a tab root can land every tap on a different screen entirely — one such chain at S3.3 opened *"Delete Day 1 and everything in it?"* and only a screenshot-before-tap habit kept the next blind tap off DELETE. Screenshot-verify the screen before any tap whose target could destroy state, and never chain more than one navigation tap without looking.
- **Every `screencap` written under `/sdcard` is media-scanned into the photo picker's grid — so driving the picker by coordinates fails in a way that blames the picker (S3.3).** Each pulled screenshot inserts a new newest tile, shifting the grid between the look and the tap; the same wrong image got picked three times before the cause surfaced, and the gallery held 185 stray agent screenshots. Write screencaps to `/data/local/tmp` (not scanned). To clean an already-polluted gallery: `rm` each file *and* broadcast `MEDIA_SCANNER_SCAN_FILE` per path — deletion alone leaves the stale MediaStore rows behind.
- **A drag-and-drop list is TWO systems claiming the same pixels — an animation and a data model — and every bug is a frame where they disagree. Remove the disagreement structurally; do not tune it away (S4.17).** The founder reported the drop as "erratic" four separate times, and each fix was a *different* disagreement, which is why chasing the symptom kept working and kept not being enough. The sequence, because the shape recurs anywhere an optimistic UI animates:
  - **The order committed at the wrong moment.** On release the code snapped the row home, cleared the neighbours' offsets, and fired the network write — whose response re-keyed the list *hundreds of milliseconds later* and moved everything a second time. **Commit the new order LOCALLY at the instant of release** (local order state + an optimistic cache write in `onMutate`, rolled back on error) and let the settle animate **only the remainder** — the sub-slot distance between the finger and the slot. The server then merely confirms what the screen already shows. This is what every polished drag library does, and it is the whole difference between "smooth" and "nearly smooth".
  - **The animation and the re-key must be pixel-neutral at the hand-off.** Web gets this for free once the order commits in the same tick as the transform conversion. **Native does not** — shared-value writes race the Fabric commit and land on the same frame *usually*, which is a probabilistic guarantee that reads as an occasional twitch. The fix is to take position away from React entirely: rows **absolutely positioned from an id→slot map held in a `useSharedValue`**, so a re-key changes each row's `index` prop while `slots[id]` — and therefore its position — does not move. The interleave is not mitigated; it is impossible.
  - **The pitch is the row height PLUS the gap, and it must be measured, not assumed.** `activityRowHeight` is a `minHeight` token, so a row with a subtitle is taller and every slot calculation downstream drifts further down the list. `onLayout` the first row and add the gap; name the value `rowPitch`, never `rowHeight`, or the next reader will "fix" it back.
  - **`Math.round(-0.5)` is `-0`, so an unguarded `Math.round(travel / pitch)` needs more travel to move UP than DOWN.** Nobody reports this — it just feels slightly wrong in one direction. Round the magnitude and re-apply the sign.
  - **The pure math is the only testable seam**, and extracting it is not optional: importing the component into Jest pulls reanimated's native init and kills the suite. `landingSlot.ts` holds the drop calculation, the displacement rule and the slot-map operations, all `'worklet'`-annotated so both threads share one definition. Its first run caught the rounding asymmetry above.
  - **Clipping instead of unmounting leaves the hidden content interactive.** A collapsed accordion body animated by height still catches pointer events and still sits in the accessibility tree — an invisible drag grip a keyboard can Tab to. Set `pointerEvents: 'none'` + `accessibilityElementsHidden` / `importantForAccessibility: 'no-hide-descendants'`, and make the preview driver skip `aria-hidden` subtrees so automation cannot operate a control the traveler cannot see.
- **An unhandled `window.confirm` HANGS the preview driver, and a tab is not a button — two ways a working control reads as broken (S4.20).** Both are properties of the harness, not of any program, so nothing Jest or `tsc` runs can catch them. **The confirm one does not fail, it blocks:** headless Chrome waits on the dialog forever, so a destructive flow (accept ownership, remove, leave) produces *no output at all* and the run has to be killed — which reads as a hung emulator or a broken backend, not as a dialog. `drive-preview.js` now stubs `window.confirm` to auto-accept beside the existing `window.alert` stub, and **prints the wording it was asked**, so the dialog stays evidence rather than becoming an invisible auto-yes. **The tab one reports `NOT FOUND`:** the click selector matched `role=button/link/checkbox/radio`, and the workspace tab row renders `role="tab"` — so every tab in it was unreachable while the app was perfectly fine, the exact S1.3 dead-click shape with the opposite cause. Selector widened. **The generalisation: when a driver says a control is missing or a run produces nothing, suspect the harness's vocabulary before the screen** — check what role the control actually renders (`accessibilityRole` in the component) rather than re-reading the handler.
- **The web drivers SHARE one Chrome profile, so the previous run's session silently becomes this run's starting state — and a polluted profile reads as a white screen (S4.20).** Every `drive-*.js` launches with `--user-data-dir=$TMPDIR/largata-publish-driver`. Sign in as a different pool traveler, or run `drive-preview.js --fresh` (which *wipes* that directory), and the next walk inherits whatever you left. S4.20 measured `drive-create-flow` at **7 passed / 28 failed** against `dev`'s **12 / 23**, spent a cycle diffing the two, and the entire gap was the profile — including a **`0 chars` page**, which is the exact signal CLAUDE.md and the driver itself teach you to read as the S0.4 white screen. **`rm -rf "$TMPDIR/largata-publish-driver"` before any run whose result you intend to compare**, and treat a 0-char page as *"which session is this?"* before *"what did I break?"* — same family as the stale-emulator-session trap: check the state the app actually has before diagnosing from the render.
- **A `git worktree` has no `node_modules`, and the smoke runner reports the resulting crash as a failed walk rather than an absent one (S4.20).** Standing up a clean worktree at `dev` to baseline a branch is the right instinct — it is how the S4.0 transport bug was isolated — but every driver there dies instantly on `Cannot find module 'ws'`, and `smoke-all.js` catches the non-zero exit and prints the script under `FAILED:` exactly as it would for a genuine assertion failure. **"The walk failed" and "the walk never started" are indistinguishable in that output**, which is the rule this repo keeps re-learning, in the harness this time. Junction the real tree's modules in (`cmd //c "mklink /J node_modules <path>"`; `npm ci` also works and costs minutes), and **confirm a baseline run produced a `N passed, M failed` line at all** before comparing anything to it. Remove the junction with **`rmdir`** before `git worktree remove`, or the delete fails with *"Filename too long"*. **Never `rm -rf` a junction — it follows the link and deletes the REAL tree's packages**, which is exactly what happened at S4.20's merge: the next check died on *"TypeScript not found"*, `npm rebuild` restored the shims but not the missing packages, and only `npm ci` fixed it. Nothing in git is at risk (`node_modules` is gitignored) but the error names the wrong thing entirely, so the minutes go to diagnosing the toolchain rather than the link you just deleted through.
- **expo-router UNMOUNTS the screen beneath a pushed one on web, so any "hold this for the whole flow" effect releases the moment a sub-screen opens — and the symptom is a blank screen, not a lost lease (S4.18).** The Draft Workspace holds the Editing Session; opening the activity form ran `useEditLock`'s unmount cleanup and released it, while an `asked` ref meant the remount never re-acquired. The editor then sat on its spinner forever. **Every programmatic probe agreed the app was fine** — the URL was right, no page error, no console error, the staged buffer intact in its module-scoped store — and the DOM simply had no day cards in it. **The discriminating signal was the backend log**: acquire at :43, release at :51, eight seconds later, exactly when the form opened. Same family as S1.6's stale-session trap — *check the state the server actually has, not the render*. Two consequences worth keeping: state that must survive the round trip cannot live in the pushing screen's component state (a module-scoped store keyed by id is the cheap fix, no dependency), and a once-only `useRef` guard around acquisition is a bug on web however correct it looks on native.
- **`usePreventRemove` exists in this app, but not where you would look for it (S4.18).** Guarding an exit — "discard unsaved changes?" on hardware back *and* web router back — is exactly what it is for, and `@react-navigation/native` is **not** a dependency here (expo-router 57 vendors its own navigation and does not declare it), so `require` fails and the obvious conclusion is that the hook is unavailable. It is not: it ships at **`expo-router/build/react-navigation/core`**, exported and typed, and works. **The wrong fix is a hand-rolled `history.pushState` trap**, which S4.18 wrote first: it desyncs expo-router's stack from the browser's, so the next `router.back()` consumes the injected duplicate instead of the screen — a pushed form never returns, and after a save the exit eats itself. Both read as component bugs. Arm it on `dirty` rather than always, or the guard intercepts its own deliberate exit.
- **A driver that reaches a screen by a route no traveler takes is testing a different app (S4.18).** `drive-buffered-plan.js` navigated straight to `/edit-plan`, so `router.back()` after a successful save had no history entry to return to and "the viewer is shown" failed against a product that was correct. Enter through the affordance (`Edit Itinerary`) and the same assertion passes. The general rule this repo keeps paying for: **when a walk fails on navigation, ask how the walk got there before reading the handler** — same family as S4.20's tab-role and shared-Chrome-profile traps, where the harness's own shortcut was the defect.
- **A closing dialog must retain the whole SUBJECT it renders from, never a field of it — retain half and the dialog tears down in two visible stages (S3.4).** `stillShowing(current, last)` exists because a `Modal` keeps painting through its dismiss animation while its data has already gone `null`. Apply it to one field and every *other* prop still re-derives from the live `null`: S3.4's photo preview retained the photo but recomputed `deletable` from a prop that was already gone, so the Delete button unmounted a frame before the dialog faded — the founder saw a button vanish under a still-visible window. **Pass one object (`DumpTile`, `MemberResponse`) and retain that**, so content and authority fade as one unit. **No automated rung can catch this**: the walk asserts the end state, and the end state was always correct — it is a property of the frames in between. Confirm by screenshotting *during* the fade (`adb shell input tap … && adb shell screencap` chained, no sleep) and checking the whole dialog is intact while the background ghosts through. The helper lives at `mobile/src/components/stillShowing.ts` — one definition, both dialogs; it was S4.20's fix and was found sitting in `profile/profileCard.ts`, which is how S3.4 nearly minted a second copy.
- **`Page.nextCursor` is `null` on the wire and `string | undefined` in the TypeScript — so `cursor !== undefined` is TRUE on an exhausted list, and the client asks for a page literally called "null" (S3.1).** The server answers `MALFORMED_CURSOR` 400, a catch-all turns it into an empty list, and the screen renders as though the traveler simply has no data — no error, no crash, nothing red. **Every layer stays green:** `tsc` is clean *because the type is the lie* (Jackson serialises the record's null, the `.d.ts` says `undefined`), every backend IT passes because they all talk to the real server, and the mobile suite passes because nothing mocked the null. It fails only in a client that walks pages against a live backend. **Compare with `??`, never `!==`** — `page.nextCursor ?? undefined` — and prefer handing `nextCursor` straight to react-query, which treats `null` as exhausted and is why the existing infinite queries never hit this. **The discriminating signal is the backend log**, one line naming `MalformedCursorException`, not the render (same family as S1.6's stale session and S4.18's released lease). A page-following loop should also carry a **repeat-cursor guard** so a server bug degrades instead of spinning: the naive version loops forever and dies on a heap OOM, which is how the test for it was verified.
- **A trailing label in a `flex: 1` row truncates on the phone frame and NOWHERE ELSE — the web preview's viewport is wider, so this class of defect is invisible on every rung except the device (S3.1).** `Add to Diary` rendered as `Add to` on the emulator while the preview drew it in full: the row is `flexDirection: 'row'` with the text block on `flex: 1`, and a `Pressable` defaults to `flexShrink: 1`, so the label loses the fight for the last pixels. **Nothing catches it but a screenshot**: Jest asserts the *string* the label helper returns (always correct — the truncation is layout, not text), `tsc` sees nothing, and the web driver's `innerText` reports the **full** label because the DOM holds it regardless of what is painted. **Fix: `flexShrink: 0` on the trailing control's wrapper** (not on the `Text` — the Pressable is what shrinks), plus `numberOfLines={1}` so any future overflow ellipsises rather than wrapping the row. **The general rule this pays for: a JS-only change still needs the device when it touches layout in a row, because "renders on web" is not "fits on a phone"** — the same shape as S1.3's dead clicks, with the opposite cause.
- *(add as discovered)*
