/**
 * The theme's one entry point. Screens import from `../src/theme`, never from `./tokens` directly —
 * one import path is what lets the module reorganise (a dark scheme, a provider, per-platform
 * values) without touching every screen.
 */
export { colors, typography, spacing, radii } from './tokens';
