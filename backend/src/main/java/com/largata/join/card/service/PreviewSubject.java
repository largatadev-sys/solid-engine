package com.largata.join.card.service;


public record PreviewSubject(
        String tripTitle,
        String metaLine,
        String imageUrl,
        String landingUrl,
        String appUrl,
        boolean live) {
}
