# REGRESSION_CHECKLIST — Largata

**The ratchet (06b §7):** every bug that escapes to a human adds a line here. Before each release candidate, run the lines below by hand (or confirm their automated coverage). **A line that recurs graduates into the Maestro smoke suite** — move it, note the flow name, strike it here.

Automated smoke suite (Maestro, per release candidate — 5–8 flows, pinned): sign in → create itinerary → invite → add item → log expense → publish. *(Flows land with their epics; the suite completes as launch scope completes.)*

**Maestro is parked, deliberately — trigger: pre-alpha, with the Play-track story** *(decided at the E1 promotion gate, 2026-07-29)*. Two reasons, each sufficient. **The ratchet's own graduation rule has never fired:** a line graduates when it *recurs*, and none has — so no flow has earned automation by the mechanism that is supposed to earn it. **And "per release candidate" describes a cadence that does not exist yet:** Maestro's job is regression-at-scale across repeated candidates shipping to real testers through the Play track, which is itself parked on the same trigger. Standing up a new toolchain (Maestro on Windows, flow files, CI emulator orchestration) to run flows once, for an audience of us, is mechanism before policy — the shape this repo has now parked three times (the entitlement seam, the EDA ladder, permanent deletion). **Meanwhile the flows are not uncovered:** the committed drivers and smoke scripts in `mobile/scripts/` (see its README) exercise the same paths on the API rung and in the preview container, with tooling that already exists.

| # | Added | Check | Origin (bug/story) | Status |
|---|-------|-------|--------------------|--------|
| 1 | 2026-07-15 | An **authenticated** request to a path that maps to no handler returns **404 + the error envelope**, never 500 / never a Spring error page. *(Amended at S0.2: an **anonymous** request to the same path now returns **401**, not 404 — default-deny rejects before routing, and that is correct: an unauthenticated prober should not learn which paths exist. Both cases are pinned by tests.)* | S0.1 — shipped as `500 INTERNAL_ERROR`; 34 tests missed it because every test hits routes that *exist*. Found by curling a nonexistent path at the story gate — and the S0.2 status change was found the same way, by the same curl at the same gate. | ✅ automated — `ErrorContractIT.unknownRouteIs404InTheEnvelope` (authenticated) + `UnauthenticatedContractIT.anUnknownRouteIs401ToAnAnonymousCaller_not404` (anonymous) |
| 2 | 2026-07-15 | **Every 401 carries a non-null `traceId`.** Assert against a real rejected request, not a mocked one — the bug lives in filter *ordering*, which only a real chain exhibits. | S0.2 — `LogContextFilter` was an unordered `@Component` (LOWEST_PRECEDENCE) while Spring Security registers at -100, so the log filter never ran for rejected requests and every 401 rendered `traceId: null`. Invisible on the happy path: a 200 gets its traceId either way. | ✅ automated — `UnauthenticatedContractIT.theEnvelopeCarriesATraceIdThatIsNotBlank` |
| 3 | 2026-07-15 | **Every flavor of bad credential returns the same envelope** — missing, expired, wrong-signature, and not-a-JWT. Testing one flavor is testing none. | S0.2 — the resource-server DSL installs its own `BearerTokenAuthenticationEntryPoint`, so only "no token at all" got our envelope; bad tokens got an empty body with a `WWW-Authenticate` header. One situation, two shapes. | ✅ automated — `UnauthenticatedContractIT.everyRejectionRendersTheStandardEnvelope` (4 flavors) |
| 4 | 2026-07-15 | **Google sign-in completes on a device** — through the real picker, to a rendered me-screen. A mock cannot stand in for this: the bug it catches lives between "the mock returns a token" and "the native SDK accepts what we built from it". | S0.2 — `GoogleAuthProvider.credential(idToken)` passed the JS layer and was rejected by the native layer (`accessToken cannot be empty`). 13 green unit tests, a working picker, and a correct SHA-1 all failed to reveal it; the first device attempt did, immediately. Fixed by passing both tokens from `getTokens()`. | ⚠ manual — needs a Google account on the AVD; unit guard is `authRepository.test.ts` "passing BOTH tokens" |
| 5 | 2026-07-15 | **The secret hook still blocks a planted real-format key** after any edit to its patterns — verify with a specimen of each format (`AIza…`, `sk_live_…`, `ghp_…`, `AKIA…`, PEM header, JWT), not just by reading the regex. | S0.2 — the exact-length patterns (`sk_live_[A-Za-z0-9]{24}`) matched only a canonical specimen; a real key one character off passed silently. Found by planting specimens while widening the hook for the auth tests' false positives. The hook is the backstop for CLAUDE.md's never-commit-secrets rule, so a silent gap in it is the worst kind. | ⚠ manual — plant specimens, confirm exit 1, remove |
| 6 | 2026-07-16 | **A nullable field the server sends as `null` is typed `\| null` in `mobile/src/types/api.ts`, never `?`** — and its test fixtures pass `null`, not an absent key. Check on every DTO that mirrors an optional backend field. | S0.3 — `startDate?: string` made `x !== undefined` true for the `null` Jackson actually sends, so an undated trip rendered a literal **"null → null"** on the device — on the one case `formatDates` exists for. 153 green tests missed it: they built objects in TypeScript, where absent *is* undefined. Same family as line 4 — a mock agreeing with our belief instead of with the wire. Found by the first device run. | ✅ automated — `formatDates.test.ts` ("never renders the words null or undefined, whatever it is handed"); the `\| null` type makes an omitted fixture field a compile error |
| 7 | 2026-07-28 | **A JPA field the domain mutates is not `@Column(updatable = false)`** — and any story that makes a previously-immutable field mutable must check the annotation before trusting a write. Verify by asserting the *column*, not the entity. | S1.6 — `Itinerary.ownerId` was `updatable = false` from S0.3, correct while ownership could not move. When transfer arrived, Hibernate silently omitted the column from every UPDATE: the entity mutated, `saveAndFlush` returned cleanly, and `owner_id` kept the old traveler with **no exception and nothing in the log**. Caught only because AC 4 asserted all four effects of a transfer together; nothing on the request path reads that column, so it would otherwise have shipped as a column quietly disagreeing with the membership rows. Same shape as V3's dead `state DEFAULT 'draft'`. | ✅ automated — `OwnershipTransferIT.acceptingAnOfferSwapsRolesSyncsTheItineraryAndRecordsTheTransfer` asserts `itinerary.owner_id` in SQL after the swap |
| 8 | 2026-07-29 | **A screen seeded from a cached list must not be marked *fresh*.** Any `useQuery` seeded from another query's cache uses **`placeholderData`, never `initialData`** — React Query stamps `initialData` as fetched-now, so with a non-zero `staleTime` the screen serves the seed and never fetches. Check on every screen that reads a row the list also carries, and check the *seed's* shape covers every field the screen reads. | E1 promotion gate — `itineraryOptions` seeded the trip screen from `itineraryKeys.list()`, which is the **unarchived** list and ships `days: []`. A trip archived by another member rendered for 30 s with no archived badge, no banner, and live Edit + Daily-schedule links, then failed with *"Someone is editing"*; a populated plan rendered as *"No days yet"*. Invisible to tests because the test QueryClient had no `staleTime` — the production config was never mirrored. | ✅ automated — `itineraryQueries.test.ts` "never serves the seeded list row as fresh", which builds its client with the production `staleTime: 30_000` and was **proven to fail** against `initialData` |
| 9 | 2026-07-29 | **An error message that names an act must be reachable only from that act.** When one exception type serves several operations, assert the *message*, not just `$.code` — a shared message is invisible to a code-only assertion. | E1 promotion gate — one `NotTripOwnerException` carrying *"Only the trip owner can remove a member."* was thrown from four operations, so a non-owner tapping **Archive** was told they could not remove a member. `TripArchiveContractIT` asserted only `$.code` and shipped green. Fixed by giving the exception a private constructor and named factories, so the compiler now enumerates every call site. | ✅ automated — the private constructor makes an unnamed throw a compile error; contract ITs assert the per-act message |
| 10 | 2026-07-29 | **No source file starts with a UTF-8 BOM (`EF BB BF`).** After any bulk edit made with a shell — especially PowerShell, whose `Set-Content -Encoding utf8` and `Out-File` write one by default — check the first three bytes of every touched file. Prefer `[System.IO.File]::WriteAllText` or the editor tools, which do not. | E1 promotion gate — a `Set-Content -Encoding utf8` rewrite of `InvitationService.java` planted a BOM. **`mvn verify` on the host passed all 309 ITs**; the container build died at `illegal character: '﻿'`. Since the Docker build is what Railway runs, this would have passed every gate we had and broken the deploy — the S0.2 `getTokens()` shape exactly: green everywhere except the environment that ships. A second BOM in `itineraryQueries.ts` passed `tsc` **and** 592 Jest tests, because TypeScript tolerates it silently. | ✅ automated — `SourceEncodingTest.noSourceFileStartsWithAByteOrderMark` (backend, walks `src` for `.java`/`.sql`/`.yml`) + `layering.test.ts` "no file starts with EF BB BF" (mobile `app`/`src`/`scripts`). **Both proven to fail** against a planted BOM |
| 11 | 2026-07-31 | **Removing navigator chrome deletes every affordance that chrome was drawing — check what the removed layer was providing, screen by screen.** After any change to `headerShown`, a tab bar, or a shared layout wrapper, walk *every* screen under it: a screen that still renders is not a screen that is still complete. Two things travel with a header specifically — the **title** and the **safe-area inset**. | S4.9 — the founder ruled headers should blend into the page, so the trip stack set `headerShown: false`. Four screens already drew their own `ScreenHeader`; **six did not**, and rendered with no heading at all while still setting `options={{ title: 'Members' }}` — a dead option that reads like a heading in the source, so the code looked right. Separately the inset vanished with the header and content slid under the camera cutout. Typecheck passed, 1369 tests passed, every screen rendered, and nothing failed; found only by opening each screen after an unrelated route move. Same family as line 8 — the failure is an *absence*, and no assertion was watching for it. | ✅ automated — `tabRouting.test.ts` enumerates every trip screen and requires (a) a `<ScreenHeader`, (b) no navigator `title`. **Proven to fail** by deleting `invite.tsx`'s header |
| 12 | 2026-07-31 | **A driver that reads `innerText` cannot see a layout defect — take the screenshot and look at it.** Any walk that asserts a screen "renders" by matching text must also capture an image, and someone must open it. Truncation, overflow, overlap and z-order are all invisible to text assertions, because the truncated label is still *in* the DOM. | S4.1 — the published-itinerary screen's five-tab bar overflowed a 393px frame and rendered **"Rev"** where "Reviews" belonged. The preview-container walk asserted all five labels and passed: `Array.from(...).innerText` returns the full string regardless of how much of it is painted. Found only by opening the screenshot the same run had already written. Same family as line 11 — the failure is *visual*, and no text assertion is watching for it. | ⚠ manual — every `drive-*.js` takes `--shot`; the discipline is opening it. `drive-publish.js`'s README section says so explicitly |
| 13 | 2026-07-31 | **A fence applied per-endpoint must be applied at *every* endpoint — enumerate the doors, do not audit by memory.** When a rule is enforced by a call the handler must remember to make, add a test that walks the handlers and fails on an unfenced one, with an explicit named exception list. | S4.1 — the audience fence (archived trips are owner-only) shipped on `GET /v1/itineraries/{id}` and `/members` but **not on `/invitations`**, so a member of an archived trip could still read its pending invitations. Every IT passed; the two fenced doors were the two anyone thought to test. Caught by code review, not by the suite — and it is the third time this repo has shipped a partial fence (the E1 gate found one "watching 2 of its 4 doors"). | ✅ automated — `AudienceFenceCoverageTest.everyWorkspaceScopedGetEitherFencesTheAudienceOrIsNamedAsAnException` walks every `*Controller.java`, plus a second test that fails when the exception list names a handler that no longer exists. **Proven to fail** by deleting one fence call — it names the exact handler |
| 14 | 2026-08-01 | **An unknown query parameter is silently ignored by Spring — so a new filter tested against a stale rung returns unfiltered data and looks like a working screen.** After adding or changing any request parameter, rebuild the rung you are testing (`docker compose up -d --build backend`) and confirm the filter's outcomes are *distinguishable* before believing the UI. `deploy-currency.js` exists to answer "is this rung running my build?" — use it. | S4.1 founder review — the Trips category chips (`?category=draft\|private\|published`) were driven on a device against a backend container built the previous day. Spring dropped the unknown param, every chip returned all 31 trips, and the screen rendered perfectly with the chip highlighted. Six ITs were green the whole time, because they run against a container built from the current source. Caught only by counting rows per category through the API and noticing all four numbers were identical — the same "a check whose two outcomes are indistinguishable" family as S1.1's deploy probe and S0.6's `/gsi/button` watcher. | ⚠ manual — rebuild the rung, then assert the counts *differ*; `TripCategoryFilterIT` covers the query itself |
| 15 | 2026-08-03 | **Editing a migration that has already run wedges Flyway on every database that ran it — so rewrite a migration only while no rung has applied it, and know which rungs those are.** Flyway stores a checksum per applied migration and refuses to start on a mismatch (`Migration checksum mismatch for migration version N`). The local stack is disposable, so the fix there is `docker compose down -v`; on a deployed rung there is no such escape, and the recovery is `flyway repair` or a data-losing wipe. Before editing any migration, ask **which databases have run it** — not which are convenient to reset. | S4.11 — V20 was rewritten (legitimately: S4.1 was unpromoted, so only throwaway local databases had ever run it), but the local Postgres **volume survived** `docker compose up --build`. The backend came up healthy-looking for three minutes, then died in `entityManagerFactory` with the mismatch buried in a Spring bean-creation stack. The container reported `Started`, `docker compose ps` showed postgres and minio `healthy`, and `curl /v1/health` simply never answered — a silence that reads as "still booting". Had S4.1 been promoted first, the same edit would have wedged deployed `dev`. | ⚠ manual — the promotion checklist asks which rungs have applied a migration before any edit; locally, `docker compose down -v` (never `up --build` alone) when a migration file changes |
| 16 | 2026-08-04 | **A sabotage check that passes is a suspect *invocation* before it is a suspect test — recompile resources before concluding a migration test has no failure mode.** `mvn failsafe:integration-test` does not run `process-resources`, so an edited `.sql` under `src/main/resources` is never copied to `target/classes` and Flyway loads the previous build's file. Always `mvn -o test-compile failsafe:integration-test …` when what you changed is a resource. | S4.13 — V21's remap was deliberately sabotaged (`WHERE state = 'COMPLETED'` instead of `'ACTIVE'`) to prove `ItineraryLifecycleRenameIT` could fail. All 8 assertions stayed green, which reads as "this test proves nothing" — the exact false conclusion the migration-test rule exists to prevent, reached by trusting the build tool rather than the test. Adding `test-compile` made the same sabotage fail 4 assertions instantly. | ⚠ manual — no test can catch it; it is a property of the Maven lifecycle, not of any program (06b §10's escape hatch). CLAUDE.md's migration gotcha carries the rule |
| 18 | 2026-08-15 | **Mail that never arrives is a *quota* question before it is a code question — and a provider dashboard reading "nothing sent" cannot tell never-called from called-and-rejected.** Before diagnosing the send path, (a) make one real call and read the HTTP status, and (b) count what the test harness spends against the same account. Transactional-mail quotas are per **account**, so local and deployed rungs share one budget — a local seeding run can silently exhaust production's. | Off-epic 2026-08-15 — registration on deployed dev sent no confirmation and an invitation never arrived, for hours. `dispatch()` catches the failure into a `log.warn`, so the traveler saw a success screen and nothing was red. The Resend dashboard showed nothing in nine hours, which reads as an outage but is ambiguous; the database proved issuance was fine (3 codes minted, 3 invitations created, `attempts = 0`), and a single real send returned the answer outright: **`429 daily_quota_exceeded`**. One `seed-travelers.js` pass fired 60 invitation emails against a 100/day account-wide limit — none of which anything ever read, since the seeders accept through the in-app inbox. Four wrong suspects were checked first (revoked key, misnamed Railway var, wrong key of two, a deploy in the window). | ⚠ manual — the seeders now invite by handle (`/invitations/by-handle` sends no mail), so the bulk spender is gone; the discipline is checking the provider's own quota before reading code |
| 17 | 2026-08-04 | **"That change was client-side, so the API smokes cannot be affected" is a judgement substituted for a check — run all three rungs, every time.** `npm run smoke` (mobile) runs the API smokes and the web-preview walks in one command and refuses to start if a rung is down; the emulator walk stays manual by design, because a driver reading `innerText` cannot see a layout defect (line 12). | S4.13 — twice in one story the founder had to ask "did you smoke test both web and mobile?", and twice the answer was no. Both times the reasoning was that the change could not affect the untested rung. The second time it hid a real gap: the publish half of the walk had never run on a device at all, only in the browser, so frame 7's success screen was unverified on the artifact that ships. Same family as lines 12 and 14 — a check skipped because its result felt predictable. | ⚠ manual — `npm run smoke` makes two of three rungs one command; the discipline is running it and then walking the device |
| 18 | 2026-08-05 | **A stack whose root is not the screen the traveler entered from can never make back correct — fix the navigator, never the call sites.** When back lands somewhere the traveler was not, check *which stack the screen lives in* before touching any `push`/`navigate`/`replace`/`backTo`. A list and the detail screens it opens must sit in one stack with the list as its root; siblings under a `Tabs` navigator are separate stacks that share no history. | S4.13 — `itineraries/`, `members/` and `published/` were three sibling tabs beside Trips, each its own `Stack`, none containing Trips. Back therefore popped into whichever trip screen had been visited earlier in that stack — a *different trip's* preview, or the day editor of the trip before. Five call-site fixes were attempted and reverted in turn (`navigate` over `push`, an `alwaysBackTo` override, a hardware-back handler, a `from` param, `dismissAll()` — which errored with *"POP_TO_TOP was not handled"*), each plausible and each unable to work, because no call site can pop onto a screen its stack does not contain. Moving the four routes into one `(trips)` group rooted at Trips fixed every reported path and **removed 45 lines net**. | ✅ automated — `tabRouting.test.ts` "roots the trip stack at Trips…" + "nests no second stack under the trip stack…" + "names no origin at all — the stack remembers it". Walked on the emulator across all four paths (draft→days, upcoming→preview, preview→days→back→back, published→back) |
| 19 | 2026-08-05 | **A Metro bundle can be stale in a way that makes a fixed bug look unfixed — probe for code you CHANGED, never for code both versions share.** After any restructure, fetch `/node_modules/expo-router/entry.bundle?platform=android&dev=true` and grep for a string the *new* code has and the old lacks (or vice versa). Restart with `--clear`, and kill the old Metro first: `expo start` exits **zero** on a port clash while `/status` keeps answering from the previous instance. | S4.13 — a first freshness check grepped for a string present in both versions and reported "fresh", so a genuinely stale bundle was trusted twice. Separately, `--clear` was issued while the old Metro still held 8082; it printed *"Port 8082 is being used… Skipping dev server"*, exited zero, and `/status` returned 200 from the **pre-restructure** process. Same family as line 14 and S0.6's `/gsi/button` watcher — a probe whose two outcomes are indistinguishable. | ⚠ manual — grep the served bundle for the changed identifier; confirm the *removed* one returns 0 hits and the *added* one returns >0 |
| 20 | 2026-08-08 | **A write that bypasses its mutation hook must invalidate the cache by hand — and a cold page load will never show you that it doesn't.** When a screen calls a repository method directly instead of through its `use*` hook, nothing runs the hook's `onSuccess`, so no query is invalidated. Verify by walking the *live session* (create → navigate → look), never by reloading: a reload refetches everything and hides the defect completely. | S4.15 — the create form uploaded the cover through `itineraryRepository.uploadCover` rather than `useUploadCover`. TanStack ran the create mutation's own `onSuccess` (invalidating the trip list) **before** the call-site handler, so the list refetched while `coverImageUrl` was still `null`, and nothing invalidated after the upload landed. Every cover was therefore invisible on the landing and the overview until a pull-to-refresh or a page reload. The story's smoke test passed because `drive-preview.js` starts each run with a fresh page load — and the overview screenshot that *did* show the placeholder was captured and never opened (line 12's lesson, re-learned). Found by the founder, minutes after the story was marked done. | ✅ automated — `tabRouting.test.ts` "TELLS THE CACHE the cover landed…" pins `onItineraryUpdated(client, withCover)` after the upload, and "navigates BEFORE the upload…" pins the ordering. Walked live-session on both rungs |
| 21 | 2026-08-08 | **The web preview container bakes its JS at build time — a code change is invisible until you rebuild the image, and the running container looks perfectly healthy.** Unlike the emulator (which takes JS from Metro and hot-reloads), the preview serves a static export. After any mobile source change, rebuild `Dockerfile.web-preview` before driving it, or confirm the bundle contains your change: `docker run --rm --entrypoint sh largata-preview:local -c "grep -rl '<your new identifier>' /srv"`. | S4.15's fix pass — three fixes were verified against a container built an hour earlier, so the driver kept showing the *pre-fix* behaviour. That reads as "the fix doesn't work" and sent the investigation into the component, through a `console.error` probe and a DB query, before the image's age was checked. Same family as line 19 (stale Metro bundle) and line 14 (stale backend) — the third rung with the same failure shape, and the only one where the staleness cannot self-heal. | ⚠ manual — rebuild before driving; grep the image's bundle for a changed identifier when a fix appears not to work |
| 22 | 2026-08-08 | **Removing a field from a FORM removes it from the WIRE when the endpoint replaces rather than merges — check the server's update semantics before culling any input.** `PATCH` is a verb, not a promise: `ActivityService.edit` calls `activity.edit(fields, …)` which `apply()`s **every** field of the request, so an omitted key writes `null`. Any form that stops showing a field must therefore keep sending it. Verify by editing a record that HAS the culled data and re-reading it — never by editing a fresh one, which has nothing to lose. | S4.17 — decision 8 culled Description, Notes & Creator Tips, Photos and the booking card from the activity form. Dropping them from the request builder would have silently wiped existing values on the first rename of any activity created before this story: no error, no log line, and the form itself could never show the loss because it no longer renders those fields. The data would only surface as missing on the published projection, days later. Caught while reading `Activity.apply()` to confirm the assumption, not by any test — every green test built its fixture in the client, where absent and null look alike (the line-6 shape). | ✅ automated — `activityRequest.test.ts` "echoes back description and notes so the PATCH does not wipe them" + "echoes back the whole booking card the form no longer edits", both against a fixture that HAS the culled values |
| 23 | 2026-08-09 | **A CSS transition dies when React moves its element in the DOM — so any transition on a row in a REORDERABLE list is a coin flip on direction.** After a keyed-list reorder, animate with the **Web Animations API** (`element.animate()`), which is attached to the element and survives reparenting, or animate a property React does not own. Verify by driving the animation in **both directions** and sampling the element's position *mid-flight*; a cancelled transition reads as already-final at the mid sample. | S4.17 — the drag settle rode a CSS `transition`. Dragging a card **down** was smooth and dragging it **up** was erratic, because React's reconciler moves the minimum set of nodes: down moves the *neighbours* around the dragged card, up moves the *dragged card itself*, and the browser cancels its running transition and applies the final style instantly. The asymmetry is the whole tell — an animation bug that depends on **direction** is a DOM-mutation bug, not an easing one. Nothing failed: no error, no console warning, tests green, and the down-drag looked perfect. Found only because the founder dragged upward on the real preview. | ✅ automated — `drive-workspace.js` "an UPWARD drag settles by easing to its slot" samples `getBoundingClientRect().top` at release, mid-settle and rest, and fails if the mid sample equals the final position (transition cancelled) or falls outside the segment |
| 24 | 2026-08-09 | **`useAnimatedStyle` applies only the keys the branch it took RETURNS — an omitted key keeps its last value forever.** Every branch must return the **same key set**, or write one return whose values are conditional. Grep the animated style for each animated property and require exactly one occurrence: a property that appears in one branch and not another has no resting value and will leak. | S4.17 — the drag's lift returned `elevation`/`opacity`/`shadowOpacity` only in the *held* branch. Two symptoms, one cause, and neither obviously the same bug: rows loaded with **no shadow** (the static style declared `shadowColor`/`offset`/`radius` but never the `shadowOpacity` that makes a shadow visible, so it was inert until a drag set it) and a dropped row **kept its shadow for the rest of the session** (nothing ever reset it). Invisible to tests — the values are on the UI thread, and no assertion in this repo can read them. | ✅ automated — `tabRouting.test.ts` "returns every animated key on EVERY branch" requires one `return` in the animated style and each of `elevation`/`opacity`/`shadowOpacity`/`backgroundColor`/`zIndex` to appear exactly once, conditioned on the held flag. **Pins the rule, not the symptom** — it fails on the next property someone forgets |
| 25 | 2026-08-09 | **A guard that lives on the only door to an act is not a guard — it is the door. Move the act, and the rule silently stops existing.** When a control moves to a new surface, ask what the *old* surface was implicitly enforcing before it got there. For any state transition on a collaboratively-edited record, the refusal belongs in the **service**, next to the mutation, never in whichever screen happens to host the button. Enumerate every sibling act at the same time: a guard applied to one transition and not its four siblings is the partial-fence shape (line 13) wearing new clothes. | S4.19 — `ItineraryService.finishPlanning` never checked the editing session, and neither did `start`, `complete`, `reopen` or `publish`. It had been safe only by accident: Finalize lived *inside* the Draft Workspace, so reaching it required holding the session. Moving Finalize to the Trip Workspace (a UI change, no backend diff) turned a latent hole into a reachable one — an owner could end planning while a member was mid-edit, and the member's editor would keep writing into a trip that was no longer a draft. Reported by the founder the same day the door opened. A parameterized IT proved the scope before the fix: **all 5 refusal cases failed, while the holder-passes and lease-expiry cases already passed**, so the guard was the only missing piece on every act. | ✅ automated — `LifecycleRespectsEditingSessionIT` runs three properties × five acts: a foreign session refuses (`EditLockedException`), the session **holder** is never blocked by their own lease, and an **expired** lease stops blocking. **Proven to fail** — the same 16 tests were red on 5 before `requireNoForeignSession` landed |
| 26 | 2026-08-09 | **"Adopted as design baseline" means RENDER THE FRAME AND COMPARE, not build from the spec's prose about the frame.** A spec that ratifies a mock's *text* changes ("title X, submit Y, placeholders simplified") is not a licence to redraw its *geometry*. Before any story that touches a mocked screen, open the archived `mock-render.html`, screenshot the shipped screen, and put them side by side — the deviations you cannot name are the ones you are shipping by accident. | S4.19 — the create form was re-housed onto a shared component using the spec's ratified strings, and four structural drifts from S4.15 frame 2 shipped unnoticed: a circled inline chevron where the mock stacks a bare one above a 28px title, "Upload photo(s)" where the mock says "Upload from camera roll", Destination and Duration stacked where the mock puts them on one `.fieldrow`, and a free-text number input where the mock draws a 120px dropdown. **Every one was in the mock's own CSS, readable in seconds.** Three code reviews and a two-rung smoke walk missed all four, because every check compared the screen to *itself* — the tests pinned the strings the spec named, and the driver confirmed the fields rendered. Found by the founder opening the mock file. | ⚠ manual — no test can compare a render to a Figma dump; the discipline is rendering the archived mock beside the shipped screen. `tabRouting.test.ts` pins the restored geometry (`styles.fieldRow`, `<OptionPicker`, `label="Destination"`, the stacked `{chrome.headline}</Text>`) so the specific drifts cannot silently return |
| 27 | 2026-08-20 | **A name added to a union is not a branch added to the switch — for any "big switch keyed by a name" component, add the coverage test the day you add the first name.** `Icon` renders `{name === 'x' && …}` branches inside one `<Svg>`, so a name whose branch never landed renders an **empty Svg**: correct size, correct position, nothing drawn, no error and no warning. The type system checks only the union, so `tsc` is clean and every `aria-label` still resolves. | S2.1 — three new icons (`barChart`, `kebab`, `checkCircleFilled`) reached the `IconName` union but their branches never landed, because the edit that added them mismatched on CRLF and silently replaced nothing. **3835 Jest tests, tsc and 24 Playwright walks were all green** while the empty state had no bar chart, the kebab had no dots and a recorded vote had no check — the walks click by `aria-label`, which is on the wrapper, not the glyph. Found only by opening a screenshot of the running preview (line 12, again). | ✅ automated — `iconCoverage.test.ts` requires a branch for every declared name **and** a declared name for every branch, plus the anti-vacuity assertion (an empty undrawn list proves nothing if the scan matched nothing). **Proven to fail** by renaming one branch — it names the exact icon in both directions |
| 28 | 2026-08-22 | **A gate that diverts a traveler owes them the way back — the ENTRY to an interrupting flow and the EXIT from it are two decisions, and only one of them is usually made.** When any gate parks an intent (a pending link, a return URL, a queued action) to run a mandatory detour, find the detour's last screen and check what it navigates to. A hardcoded destination there discards the intent silently. Prefer one exported "where does a settled traveler land" function called from both the gate and the detour's exit, so the two cannot drift. | S4.28 — a signed-out traveler opening a `/join/<token>` link had the token stashed correctly, and the gate honoured it correctly, but only via `settledHomeOf`, which `destinationFor` reaches **only when `nextOnboardingStep()` returns null**. A traveler who still owed onboarding was therefore sent to onboarding (right) and then dumped on Home by `complete.tsx`'s hardcoded `router.replace(SIGNED_IN_HOME)` (wrong) — the postcard lost, the token left in `localStorage` forever. The suite named the gap without covering it: `joinGate.test.ts` had a test called *"finishes onboarding **before** it honours the link"*, and nothing asserted it was honoured afterwards. 4553 Jest tests and `tsc` were green throughout; found by the founder clicking their own invite link. Same family as line 25 — a rule that held only because of where a screen happened to sit. | ✅ automated — `onboardingExit.test.ts` scans `app/onboarding/` and fails any screen that routes to `SIGNED_IN_HOME` on its own authority, plus pins `landingAfterSignIn` and `forgetPendingJoin` in `complete.tsx`. **Proven to fail** — the original hardcoded exit turns 2 of its 8 red. `joinGate.test.ts` covers the destination rule and the cold-storage hold |
| 29 | 2026-08-24 | **`scrollToOffset`/`scrollTo` with `animated: true` silently does NOTHING on react-native-web — any "scroll back to the top" control is dead in the browser while working perfectly on the device.** After adding or moving one, check the flag: it must come from `src/navigation/scrollToTop.ts` (native `true`, web `false`), never be a literal `true`. Verify by scrolling the real container and reading `scrollTop` after the tap, not by watching the screen — the 2px it does move looks like the start of an animation. | S4.34 — retapping Trips scrolled down took the *correct* branch (a probe confirmed the right tab, a populated `FlatList` ref, and no toast) and the list still did not move: `600 → 598`, which is the `onScroll` write-back rather than an animation. `animated: false` scrolled the identical call `600 → 0`. Home's `toTop(true)` had carried the same defect since S4.22 and nobody saw it, because Home's fixture has nothing to scroll — its walk *skips* rather than fails, so the bug and its absence were indistinguishable. Same family as S4.21's `pagingEnabled` and S4.22's `nativeEvent.timestamp`: an RN web fork that quietly does less than its types promise, invisible to `tsc` and to every Jest suite. | ✅ automated — `tabRetap.test.ts` ("no retap surface passes a bare animated:true") fails any surface that hardcodes the flag, and pins both forks' values. ⚠ the *behaviour* still needs a browser: only the Trips walk has content to scroll, so Home/Discover/Profile are proven on the device |
| 30 | 2026-08-26 | **An event whose audience is narrower than its topic must carry NO payload — and the only test that proves it is now skipped, so nothing is watching.** Before adding a payload to any socket frame, ask who is subscribed to the topic versus who is allowed to see the data: a trip topic has every member on it, while the join-request queue is owner-only today. The rule is S4.35's design decision 5, and it is invisible on every screen — no UI shows you that a member's client received a frame and did nothing with it. | S4.36's gate — `live-travelers.spec.ts:56` is the guard, and it failed in CI (green locally in ~8s) at its **setup**, not its assertion: `trackApiTraffic` waits for a quiet second, and its `/v1/` filter counts the socket's own `POST /v1/ws-ticket` handshake. On a slow runner the reconnect backoff (1s→2s→4s, capped 30s) keeps landing inside the sampling window — each reconnect fires a ticket **and** `markStaleOnReconnect`, which refetches — so the count never stops moving and the 45s budget expires. It failed from before S4.36 branched (proven at `fb73bd7`, the branch point) and no S4.36 file touches that surface. **The privacy invariant is unguarded in practice**: add a payload to the join-queue frame today and every member of the trip learns who asked to join, with nothing red to stop it. | ⚠ **UNGUARDED** — `test.skip`ped 2026-08-26 by founder call, with the reason in the skip title so CI reports it rather than staying permanently red and teaching everyone to ignore the signal. **Two things must happen before it returns**, both epic-map lines: the harness fix (stop counting the handshake; settle on N consecutive quiet samples), and a decision on whether the assertion still describes the product — the founder has queued **opening the join queue to every member**, which would retire this premise outright. Owned by S4.35's ticket 08 |
| 31 | 2026-08-26 | **A screen shipped behind two doors is only reachable through the doors that exist on launch day — walk every path a traveler would take to a new surface, not just the paths the story built.** When a story adds a destination (a results screen, a detail view), enumerate the gestures that *should* reach it — including the ones owned by neighbouring code the story didn't touch — and drive each one. A conditional door (`morePeople &&`) whose condition can't fire at current data scale is indistinguishable from no door. | S4.36 shipped People search complete and proven — suggestions group, dedicated results screen, fences, Playwright walks green — and the founder still couldn't find anyone: **executing** a search routes to the itinerary results screen (which contains no people), and the only doors to the People screen sat inside the suggestions overlay, one of them ("See all people") gated on *more than 3 matches*, which the current traveler count never produces. Nothing was broken; the natural gesture just wasn't wired, and no walk drove type-then-submit for a person. Found by the founder within a day of merge. Same family as line 28 — the entry built, the path a real person takes unowned. | ✅ automated — S4.37 shipped C8: submit lands on the combined People + Trips screen and the >3 gate dropped to ≥1. `e2e/web/follow.spec.ts` drives type → **submit** → the person is visible, and asserts "See all people" renders against a single match; `combinedResults.test.ts` pins the ≥1 rule and the singular count line |
| 32 | 2026-08-26 | **A test that documents its own unreliability still fails CI on somebody else's branch — quarantine it or make it deterministic, but do not leave it voting.** `SlowConsumerIsolationIT` carries an assertion message saying it is *"NOT YET A PROOF… kept as the harness the decision needs, and must be made to fail before it may be cited as evidence for anything"* — an honest note about a test that cannot yet prove what it is named for. But it is a normal `*IT`, so failsafe runs it and its verdict gates the build like any other. | S4.37 — the PR run on `57551a3` failed with `Tests run: 1061, Failures: 1` on that test alone, on a branch that touches no websocket code; the **same commit's push run passed**, and both PR runs since have passed. The cost is not the red build, it is the minutes spent proving the failure was unrelated — the diagnosis has to be done from scratch each time, by someone who has no context on a story they did not write. Same family as line 6's indistinguishable outcomes: a flaky gate and a real regression look identical in the runner's summary. | ⚠ manual — when it reddens a branch that touches no socket code, read the assertion message before diagnosing. **The fix is to stop it voting**: rename it out of the `*IT` pattern (it stays runnable by name), or give it a deterministic trigger. Epic-map line owns the decision |
